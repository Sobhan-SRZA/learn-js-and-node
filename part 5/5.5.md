# فصل ۲۶

# Set

## مقدمه

تا اینجا با **Array** و **Object** آشنا شدید و یاد گرفتید چگونه مجموعه‌ای از داده‌ها را مدیریت کنید.

اما گاهی نیاز داریم مجموعه‌ای از داده‌ها را نگهداری کنیم که **هیچ مقدار تکراری** در آن وجود نداشته باشد.

برای مثال:

* لیست کاربران آنلاین
* شناسه کاربران
* تگ‌های یک مقاله
* دسته‌بندی محصولات
* لیست مجوزها (Permissions)
* نقش‌های کاربران (Roles)

اگر از Array استفاده کنیم، باید خودمان قبل از اضافه کردن هر مقدار بررسی کنیم که قبلاً وجود نداشته باشد.

JavaScript ساختمان داده‌ای به نام **Set** را برای حل این مشکل ارائه کرده است.

Set مجموعه‌ای از مقادیر **Unique** (غیرتکراری) است.

اگر یک مقدار چند بار به Set اضافه شود، فقط یک نسخه از آن نگهداری می‌شود.

در پروژه‌های واقعی Node.js، Set یکی از بهترین ابزارها برای حذف داده‌های تکراری، مدیریت شناسه‌ها، کش کردن اطلاعات، بررسی عضویت و افزایش سرعت جستجو است.

در این فصل تمام قابلیت‌های Set را از پایه تا کاربردهای واقعی یاد خواهید گرفت.

---

# فهرست فصل

## بخش ۱

* معرفی Set
* ایجاد Set
* ساختار Set
* تفاوت Set و Array
* ویژگی‌های Set
* افزودن مقدار
* حذف مقدار
* بررسی وجود مقدار
* تعداد اعضا
* پاک کردن کامل Set

---

## بخش ۲

* متدهای مهم Set
* add()
* delete()
* has()
* clear()
* keys()
* values()
* entries()
* forEach()

---

## بخش ۳

* پیمایش Set
* for...of
* Spread Operator
* تبدیل Set به Array
* تبدیل Array به Set
* حذف مقادیر تکراری
* کار با String و Set

---

## بخش ۴

* عملیات روی Set
* Union
* Intersection
* Difference
* Symmetric Difference
* Subset
* Superset
* کاربردهای عملی

---

## بخش ۵

* اشتباهات رایج

---

## بخش ۶

* نکات حرفه‌ای (Best Practices)

---

## بخش ۷

* تمرین

---

## بخش ۸

* پاسخ تشریحی تمرین‌ها

---

## بخش ۹

* مینی‌پروژه

---

## مطالبی که در پایان این فصل یاد می‌گیرید

* ایجاد Set
* اضافه کردن اعضا
* حذف اعضا
* بررسی وجود داده
* حذف داده‌های تکراری
* تبدیل Set و Array به یکدیگر
* پیمایش اعضای Set
* استفاده از متدهای مهم Set
* انجام عملیات مجموعه‌ها
* استفاده از Set در پروژه‌های واقعی Node.js
* تشخیص اشتباهات رایج
* نوشتن کدهای استاندارد و حرفه‌ای با Set

---

# بخش ۱

# آشنایی با Set

تا قبل از معرفی Set، برای نگهداری چند مقدار معمولاً از Array استفاده می‌کردیم.

```javascript id="d8r4mk"
const numbers = [10, 20, 30];
```

اما Array یک مشکل دارد.

می‌تواند مقادیر تکراری را نگهداری کند.

```javascript id="w7m1tx"
const numbers = [10, 20, 10, 30, 20];

console.log(numbers);
```

خروجی

```text id="g3q8pa"
[10, 20, 10, 30, 20]
```

گاهی وجود داده‌های تکراری باعث ایجاد مشکل در برنامه می‌شود.

برای مثال:

* لیست کاربران آنلاین
* شناسه محصولات
* تگ‌های یک مقاله
* ایمیل کاربران
* نقش‌های کاربر

در چنین شرایطی باید مطمئن باشیم هر مقدار فقط یک بار ذخیره شود.

اینجاست که **Set** وارد می‌شود.

---

# Set چیست؟

Set یک ساختمان داده (Data Structure) در JavaScript است که فقط **مقادیر یکتا (Unique Values)** را نگهداری می‌کند.

اگر یک مقدار چند بار به Set اضافه شود، فقط اولین مقدار ذخیره می‌شود.

---

# ایجاد Set

برای ساخت یک Set از کلاس `Set` استفاده می‌کنیم.

```javascript id="p4v9ka"
const numbers = new Set();
```

اکنون یک Set خالی ساخته شده است.

---

# نمایش Set

```javascript id="h6m2tw"
const numbers = new Set();

console.log(numbers);
```

خروجی

```text id="v1q8rb"
Set(0) {}
```

عدد داخل پرانتز تعداد اعضای Set را نشان می‌دهد.

---

# ایجاد Set همراه با مقدار اولیه

می‌توان هنگام ساخت Set، یک Array به آن داد.

```javascript id="n8p3mx"
const numbers = new Set([
    10,
    20,
    30
]);

console.log(numbers);
```

خروجی

```text id="e7t5pk"
Set(3) {10, 20, 30}
```

---

# حذف خودکار مقادیر تکراری

```javascript id="u2m6rv"
const numbers = new Set([
    10,
    20,
    20,
    30,
    30,
    10
]);

console.log(numbers);
```

خروجی

```text id="b9k4tn"
Set(3) {10, 20, 30}
```

Set تمام مقادیر تکراری را حذف کرده است.

---

# مثال دیگر

```javascript id="r5m7qx"
const fruits = new Set([
    "Apple",
    "Banana",
    "Apple",
    "Orange",
    "Banana"
]);

console.log(fruits);
```

خروجی

```text id="j4v2pc"
Set(3) {
    "Apple",
    "Banana",
    "Orange"
}
```

---

# تفاوت Set و Array

| Array                    | Set                         |
| ------------------------ | --------------------------- |
| مقادیر تکراری مجاز هستند | مقادیر تکراری حذف می‌شوند   |
| دارای اندیس (Index) است  | اندیس ندارد                 |
| دسترسی با شماره اندیس    | دسترسی با پیمایش            |
| مناسب نگهداری ترتیب      | مناسب نگهداری داده‌های یکتا |

---

# ویژگی‌های Set

یک Set دارای ویژگی‌های زیر است.

* فقط مقادیر یکتا را نگهداری می‌کند.
* ترتیب ورود اعضا حفظ می‌شود.
* اندیس ندارد.
* طول آن با `size` مشخص می‌شود.
* اعضا می‌توانند از هر نوع داده‌ای باشند.

---

# اعضای Set می‌توانند از هر نوع باشند

اعداد

```javascript id="f8m1pk"
const numbers = new Set([
    1,
    2,
    3
]);
```

---

رشته

```javascript id="k2q6ra"
const names = new Set([
    "Ali",
    "Sara",
    "Reza"
]);
```

---

Boolean

```javascript id="m7v4tb"
const flags = new Set([
    true,
    false
]);
```

---

Object

```javascript id="q9p3mx"
const users = new Set([
    {
        name: "Ali"
    },
    {
        name: "Sara"
    }
]);
```

---

Array

```javascript id="y5r8kc"
const values = new Set([
    [1, 2],
    [3, 4]
]);
```

---

حتی می‌توان انواع مختلف داده را کنار هم قرار داد.

```javascript id="a6m9pv"
const data = new Set([
    10,
    "Ali",
    true,
    null
]);

console.log(data);
```

---

# اضافه کردن مقدار

برای اضافه کردن عضو جدید از متد `add()` استفاده می‌کنیم.

```javascript id="c1q7mr"
const numbers = new Set();

numbers.add(10);

console.log(numbers);
```

خروجی

```text id="w8m2tx"
Set(1) {10}
```

---

چند مقدار

```javascript id="h4p5ra"
const numbers = new Set();

numbers.add(10);

numbers.add(20);

numbers.add(30);

console.log(numbers);
```

---

اگر مقدار تکراری باشد

```javascript id="t6m3qb"
const numbers = new Set();

numbers.add(10);

numbers.add(10);

numbers.add(10);

console.log(numbers);
```

خروجی

```text id="r9v1pk"
Set(1) {10}
```

عضو جدید اضافه نمی‌شود.

---

# حذف مقدار

برای حذف عضو از `delete()` استفاده می‌کنیم.

```javascript id="p8q4mv"
const numbers = new Set([
    10,
    20,
    30
]);

numbers.delete(20);

console.log(numbers);
```

خروجی

```text id="z3m7tn"
Set(2) {10, 30}
```

---

اگر مقدار وجود نداشته باشد

```javascript id="x2v8ra"
const numbers = new Set([
    10,
    20
];

numbers.delete(100);

console.log(numbers);
```

خروجی

```text id="l5q9pk"
Set(2) {10, 20}
```

هیچ خطایی ایجاد نمی‌شود.

> **نکته:** در مثال بالا یک `)` جا افتاده است. نسخه صحیح:

```javascript id="h8r2mw"
const numbers = new Set([
    10,
    20
]);

numbers.delete(100);

console.log(numbers);
```

---

# بررسی وجود مقدار

از متد `has()` استفاده می‌کنیم.

```javascript id="m5p1tx"
const numbers = new Set([
    10,
    20,
    30
]);

console.log(
    numbers.has(20)
);
```

خروجی

```text id="e6v4rb"
true
```

---

اگر مقدار وجود نداشته باشد

```javascript id="g7m9pk"
console.log(
    numbers.has(100)
);
```

خروجی

```text id="n1q5tc"
false
```

---

# تعداد اعضای Set

برخلاف Array که از `length` استفاده می‌کند، در Set از `size` استفاده می‌شود.

```javascript id="k8r6pv"
const numbers = new Set([
    10,
    20,
    30
]);

console.log(
    numbers.size
);
```

خروجی

```text id="b4m2qx"
3
```

---

مثال

```javascript id="v9p3ka"
const fruits = new Set([
    "Apple",
    "Banana",
    "Orange"
]);

console.log(
    fruits.size
);
```

---

# حذف تمام اعضا

از متد `clear()` استفاده می‌کنیم.

```javascript id="s2m7rv"
const numbers = new Set([
    10,
    20,
    30
]);

numbers.clear();

console.log(numbers);
```

خروجی

```text id="j8q1tm"
Set(0) {}
```

---

# آیا Set ترتیب اعضا را حفظ می‌کند؟

بله.

ترتیب اضافه شدن اعضا حفظ می‌شود.

```javascript id="u5v9pk"
const colors = new Set();

colors.add("Red");
colors.add("Blue");
colors.add("Green");

console.log(colors);
```

خروجی

```text id="q2m6rb"
Set(3) {
    "Red",
    "Blue",
    "Green"
}
```

---

# آیا Set اندیس دارد؟

خیر.

کد زیر اشتباه است.

```javascript id="x4t8pa"
const numbers = new Set([
    10,
    20,
    30
]);

console.log(
    numbers[0]
);
```

خروجی

```text id="c9p1mx"
undefined
```

برای دسترسی به اعضا باید Set را پیمایش کنید که در بخش بعدی یاد خواهید گرفت.

---

# مقایسه مقدارهای اولیه و Objectها

در مقدارهای اولیه (Primitive)، مقادیر تکراری حذف می‌شوند.

```javascript id="d3m5tv"
const set = new Set([
    10,
    10,
    10
]);

console.log(set.size);
```

خروجی

```text id="y6q8rk"
1
```

اما برای Objectها، هر Object یک مرجع (Reference) مستقل دارد.

```javascript id="f2v7mc"
const set = new Set();

set.add({
    name: "Ali"
});

set.add({
    name: "Ali"
});

console.log(set.size);
```

خروجی

```text id="r7m4qb"
2
```

با وجود اینکه محتوا یکسان است، این دو Object متفاوت محسوب می‌شوند.

---

# چه زمانی از Set استفاده کنیم؟

از Set استفاده کنید زمانی که:

* داده‌های تکراری نباید ذخیره شوند.
* سرعت بررسی وجود یک مقدار اهمیت دارد.
* قصد حذف مقادیر تکراری یک Array را دارید.
* می‌خواهید مجموعه‌ای از شناسه‌ها یا مقادیر یکتا را نگهداری کنید.
* با نقش‌ها، مجوزها یا تگ‌های یکتا کار می‌کنید.

---

# خلاصه

| ویژگی                      | Set        |
| -------------------------- | ---------- |
| حذف خودکار داده‌های تکراری | ✅          |
| ترتیب ورود اعضا            | ✅          |
| اندیس                      | ❌          |
| تعداد اعضا                 | `size`     |
| افزودن عضو                 | `add()`    |
| حذف عضو                    | `delete()` |
| بررسی وجود عضو             | `has()`    |
| حذف همه اعضا               | `clear()`  |

---

# جمع‌بندی بخش اول

در این بخش با مفاهیم پایه Set آشنا شدید.

اکنون می‌توانید:

* یک Set ایجاد کنید.
* اعضای جدید به آن اضافه کنید.
* اعضا را حذف کنید.
* وجود یک مقدار را بررسی کنید.
* تعداد اعضا را به دست آورید.
* تمام اعضای Set را حذف کنید.
* تفاوت‌های اصلی بین Set و Array را تشخیص دهید.

در بخش بعدی با تمام متدهای مهم Set و نحوه استفاده از آن‌ها در پروژه‌های واقعی Node.js آشنا خواهید شد.

---

# بخش ۲

# متدهای مهم Set

در بخش قبل با ایجاد Set و عملیات پایه آشنا شدید.

در این بخش تمام متدهای مهم Set را به صورت کامل بررسی می‌کنیم.

متدهایی که یاد خواهید گرفت:

* add()
* delete()
* has()
* clear()
* keys()
* values()
* entries()
* forEach()

---

# متد add()

از متد `add()` برای اضافه کردن عضو جدید به Set استفاده می‌شود.

---

## ساختار

```javascript id="f8q3tm"
set.add(value);
```

---

## مثال

```javascript id="r2m9pk"
const numbers = new Set();

numbers.add(10);

console.log(numbers);
```

خروجی

```text id="v7k1ra"
Set(1) {10}
```

---

## اضافه کردن چند مقدار

```javascript id="g4m6tx"
const numbers = new Set();

numbers.add(10);
numbers.add(20);
numbers.add(30);

console.log(numbers);
```

خروجی

```text id="d5q8pv"
Set(3) {10, 20, 30}
```

---

## اضافه کردن مقدار تکراری

```javascript id="k9v2rb"
const numbers = new Set();

numbers.add(10);
numbers.add(10);
numbers.add(10);

console.log(numbers);
```

خروجی

```text id="a3m7qc"
Set(1) {10}
```

هیچ عضو تکراری اضافه نمی‌شود.

---

## قابلیت زنجیره‌ای (Method Chaining)

متد `add()` خود Set را برمی‌گرداند.

بنابراین می‌توان چندین بار آن را پشت سر هم فراخوانی کرد.

```javascript id="w8p4tn"
const numbers = new Set();

numbers
    .add(10)
    .add(20)
    .add(30);

console.log(numbers);
```

---

# متد delete()

برای حذف یک عضو استفاده می‌شود.

---

## ساختار

```javascript id="e1r9pk"
set.delete(value);
```

---

## مثال

```javascript id="q5m2tv"
const numbers = new Set([
    10,
    20,
    30
]);

numbers.delete(20);

console.log(numbers);
```

خروجی

```text id="n4q7ra"
Set(2) {10, 30}
```

---

## مقدار بازگشتی

اگر عضو حذف شود

```javascript id="j7m5px"
const numbers = new Set([
    10,
    20
]);

console.log(
    numbers.delete(20)
);
```

خروجی

```text id="x9k2tb"
true
```

اگر عضو وجود نداشته باشد

```javascript id="b3v6mq"
console.log(
    numbers.delete(100)
);
```

خروجی

```text id="c8p1rw"
false
```

---

# متد has()

برای بررسی وجود یک مقدار در Set استفاده می‌شود.

---

## ساختار

```javascript id="m2t8ka"
set.has(value);
```

---

## مثال

```javascript id="p6q3mx"
const users = new Set([
    "Ali",
    "Sara",
    "Reza"
]);

console.log(
    users.has("Sara")
);
```

خروجی

```text id="t5m7rv"
true
```

---

اگر مقدار وجود نداشته باشد

```javascript id="u4p9kc"
console.log(
    users.has("Amir")
);
```

خروجی

```text id="h1q6tb"
false
```

---

## بررسی قبل از اضافه کردن

```javascript id="z8m2pn"
const users = new Set();

const username = "Ali";

if (!users.has(username)) {
    users.add(username);
}
```

---

# متد clear()

تمام اعضای Set را حذف می‌کند.

---

## ساختار

```javascript id="n7v5qa"
set.clear();
```

---

## مثال

```javascript id="k4m1tx"
const numbers = new Set([
    10,
    20,
    30
]);

numbers.clear();

console.log(numbers);
```

خروجی

```text id="r6p8mv"
Set(0) {}
```

---

## مقدار بازگشتی

```javascript id="y9m4tb"
const result =
    numbers.clear();

console.log(result);
```

خروجی

```text id="g2v7qc"
undefined
```

---

# متد keys()

در Set، متد `keys()` تمام اعضا را برمی‌گرداند.

برخلاف Object، در Set کلید جداگانه‌ای وجود ندارد.

---

## مثال

```javascript id="w3q1pk"
const colors = new Set([
    "Red",
    "Blue",
    "Green"
]);

console.log(
    colors.keys()
);
```

خروجی یک Iterator است.

---

برای مشاهده اعضا

```javascript id="d8m6ra"
for (const color of colors.keys()) {
    console.log(color);
}
```

خروجی

```text id="j5t2pv"
Red
Blue
Green
```

---

# متد values()

تمام مقدارهای Set را برمی‌گرداند.

---

```javascript id="f4m8tk"
const colors = new Set([
    "Red",
    "Blue",
    "Green"
]);

for (const color of colors.values()) {
    console.log(color);
}
```

خروجی

```text id="v3q7rc"
Red
Blue
Green
```

---

## تفاوت keys و values

در Set هیچ تفاوتی وجود ندارد.

```javascript id="m9p2kb"
const set = new Set([
    1,
    2,
    3
]);

console.log(
    set.keys() === set.values()
);
```

خروجی

```text id="q4m5tx"
false
```

هر دو Iterator جداگانه تولید می‌کنند، اما محتوای آن‌ها یکسان است.

---

# متد entries()

در Array و Object این متد کلید و مقدار را برمی‌گرداند.

اما در Set کلیدی وجود ندارد.

بنابراین مقدار دو بار بازگردانده می‌شود.

---

## مثال

```javascript id="x2r8pa"
const numbers = new Set([
    10,
    20
]);

for (const entry of numbers.entries()) {
    console.log(entry);
}
```

خروجی

```text id="b6m9qc"
[10, 10]
[20, 20]
```

---

استفاده با Destructuring

```javascript id="k7v4pn"
for (const [key, value] of numbers.entries()) {
    console.log(key, value);
}
```

خروجی

```text id="h3q1ra"
10 10
20 20
```

---

# متد forEach()

مانند Array می‌توان روی تمام اعضای Set پیمایش انجام داد.

---

## ساختار

```javascript id="r8m5tv"
set.forEach(callback);
```

---

## مثال

```javascript id="n5q2pk"
const numbers = new Set([
    10,
    20,
    30
]);

numbers.forEach(function (value) {

    console.log(value);

});
```

خروجی

```text id="u7p4mx"
10
20
30
```

---

استفاده از Arrow Function

```javascript id="c1m8ra"
numbers.forEach(value => {
    console.log(value);
});
```

---

## پارامترهای Callback

```javascript id="z4v6tb"
numbers.forEach(function (
    value,
    key,
    set
) {

    console.log(value);
    console.log(key);
    console.log(set);

});
```

---

نکته مهم

در Set پارامتر دوم (`key`) برابر با `value` است.

```javascript id="a9q3pk"
numbers.forEach(
    (value, key) => {

        console.log(value);

        console.log(key);

    }
);
```

خروجی

```text id="m2t7rv"
10
10

20
20

30
30
```

---

# استفاده از size

اگرچه `size` یک متد نیست، اما یکی از پرکاربردترین ویژگی‌های Set است.

```javascript id="p5m1qx"
const numbers = new Set([
    10,
    20,
    30
]);

console.log(
    numbers.size
);
```

خروجی

```text id="w8q4tb"
3
```

---

# مثال واقعی

حذف ایمیل‌های تکراری

```javascript id="g7m2pn"
const emails = new Set();

emails.add("ali@test.com");
emails.add("sara@test.com");
emails.add("ali@test.com");

console.log(emails);
```

---

# مثال واقعی

بررسی مجوز کاربر

```javascript id="d3v9ka"
const permissions = new Set([
    "read",
    "write",
    "delete"
]);

if (
    permissions.has("delete")
) {
    console.log("Access Granted");
}
```

---

# مثال واقعی

خالی کردن کش

```javascript id="y6m4rc"
const cache = new Set([
    "user-1",
    "user-2"
]);

cache.clear();

console.log(cache.size);
```

---

# خلاصه متدها

| متد         | کاربرد                                |
| ----------- | ------------------------------------- |
| `add()`     | اضافه کردن عضو                        |
| `delete()`  | حذف عضو                               |
| `has()`     | بررسی وجود عضو                        |
| `clear()`   | حذف تمام اعضا                         |
| `keys()`    | دریافت Iterator اعضا                  |
| `values()`  | دریافت Iterator مقدارها               |
| `entries()` | دریافت Iterator شامل `[value, value]` |
| `forEach()` | پیمایش اعضا                           |
| `size`      | تعداد اعضا                            |

---

# جمع‌بندی بخش دوم

در این بخش با تمام متدهای اصلی Set آشنا شدید.

اکنون می‌توانید:

* اعضای جدید اضافه کنید.
* اعضا را حذف کنید.
* وجود یک عضو را بررسی کنید.
* کل Set را پاک کنید.
* اعضای Set را با Iteratorها دریافت کنید.
* با `forEach()` روی تمام اعضا پیمایش انجام دهید.
* از ویژگی `size` برای به دست آوردن تعداد اعضا استفاده کنید.

در بخش بعدی با پیمایش Set، تبدیل Set و Array به یکدیگر و حذف مقادیر تکراری با استفاده از Set آشنا خواهید شد.

---

# بخش ۳

# پیمایش Set و تبدیل بین Set و Array

در بخش قبل با متدهای اصلی Set آشنا شدید.

در این بخش یاد می‌گیرید چگونه روی اعضای Set پیمایش کنید، آن را به Array تبدیل کنید، Array را به Set تبدیل کنید و از Set برای حذف مقادیر تکراری استفاده کنید.

مطالب این بخش:

* پیمایش با `for...of`
* تبدیل Set به Array
* تبدیل Array به Set
* حذف مقادیر تکراری
* Spread Operator
* کار با String و Set

---

# پیمایش Set با for...of

رایج‌ترین روش برای پیمایش Set استفاده از حلقه `for...of` است.

```javascript id="x2k7ma"
const numbers = new Set([
    10,
    20,
    30
]);

for (const number of numbers) {
    console.log(number);
}
```

خروجی

```text id="b4v8rc"
10
20
30
```

---

مثال دیگر

```javascript id="m8q1tp"
const colors = new Set([
    "Red",
    "Blue",
    "Green"
]);

for (const color of colors) {
    console.log(color);
}
```

---

# ترتیب پیمایش

Set ترتیب ورود اعضا را حفظ می‌کند.

```javascript id="q6r3mk"
const users = new Set();

users.add("Ali");
users.add("Sara");
users.add("Reza");

for (const user of users) {
    console.log(user);
}
```

خروجی

```text id="n5m2pa"
Ali
Sara
Reza
```

اعضا دقیقاً به همان ترتیبی که اضافه شده‌اند پیمایش می‌شوند.

---

# تبدیل Set به Array

گاهی لازم است از متدهای Array مانند `map()` یا `filter()` استفاده کنید.

برای این کار ابتدا باید Set را به Array تبدیل کنید.

---

## روش اول

استفاده از Spread Operator

```javascript id="y9t4kv"
const numbers = new Set([
    10,
    20,
    30
]);

const array = [...numbers];

console.log(array);
```

خروجی

```text id="f7q6rb"
[10, 20, 30]
```

---

## روش دوم

استفاده از Array.from()

```javascript id="r3m8qc"
const numbers = new Set([
    10,
    20,
    30
]);

const array =
    Array.from(numbers);

console.log(array);
```

خروجی

```text id="c1v5tp"
[10, 20, 30]
```

---

# تفاوت دو روش

هر دو نتیجه یکسانی تولید می‌کنند.

```javascript id="j7p2mx"
const set = new Set([
    1,
    2,
    3
]);

console.log([...set]);

console.log(
    Array.from(set)
);
```

در پروژه‌های امروزی معمولاً از Spread Operator بیشتر استفاده می‌شود.

---

# تبدیل Array به Set

برای حذف مقادیر تکراری یا استفاده از قابلیت‌های Set می‌توان یک Array را به Set تبدیل کرد.

```javascript id="g5m9ra"
const numbers = [
    10,
    20,
    30
];

const set =
    new Set(numbers);

console.log(set);
```

خروجی

```text id="w8q4pn"
Set(3) {10, 20, 30}
```

---

# حذف مقادیر تکراری

یکی از مهم‌ترین کاربردهای Set حذف مقادیر تکراری از Array است.

```javascript id="u2v7tk"
const numbers = [
    10,
    20,
    10,
    30,
    20,
    40
];

const unique =
    [...new Set(numbers)];

console.log(unique);
```

خروجی

```text id="k3m1qb"
[10, 20, 30, 40]
```

---

مثال دیگر

```javascript id="p9r6mc"
const fruits = [
    "Apple",
    "Banana",
    "Apple",
    "Orange",
    "Banana"
];

const uniqueFruits =
    [...new Set(fruits)];

console.log(uniqueFruits);
```

خروجی

```text id="h6t8pa"
[
    "Apple",
    "Banana",
    "Orange"
]
```

---

# حذف حروف تکراری یک رشته

```javascript id="v4m2qx"
const text = "javascript";

const result =
    [...new Set(text)];

console.log(result);
```

خروجی

```text id="d8p5rc"
[
    "j",
    "a",
    "v",
    "s",
    "c",
    "r",
    "i",
    "p",
    "t"
]
```

---

اگر بخواهید دوباره رشته بسازید

```javascript id="e1q9tb"
const text = "javascript";

const result =
    [...new Set(text)]
        .join("");

console.log(result);
```

خروجی

```text id="m7v3pk"
javscript
```

---

# تبدیل String به Set

از آنجایی که String قابل پیمایش (Iterable) است، می‌توان مستقیماً آن را به Set تبدیل کرد.

```javascript id="q5m8ra"
const letters =
    new Set("NodeJS");

console.log(letters);
```

خروجی

```text id="x2p6tv"
Set(6) {
    "N",
    "o",
    "d",
    "e",
    "J",
    "S"
}
```

---

اگر حروف تکراری وجود داشته باشند

```javascript id="z9r4mk"
const letters =
    new Set("banana");

console.log(letters);
```

خروجی

```text id="g1v7qc"
Set(3) {
    "b",
    "a",
    "n"
}
```

---

# تبدیل Set به String

ابتدا Set را به Array تبدیل کرده و سپس از `join()` استفاده کنید.

```javascript id="k6m1pa"
const letters = new Set([
    "N",
    "o",
    "d",
    "e"
]);

const text =
    [...letters].join("");

console.log(text);
```

خروجی

```text id="p3q8rb"
Node
```

---

# استفاده از متدهای Array روی Set

Set متدهایی مانند `map()` یا `filter()` ندارد.

کد زیر اشتباه است.

```javascript id="a8v5tm"
const numbers = new Set([
    10,
    20,
    30
]);

numbers.map(number => number * 2);
```

خروجی

```text id="r5m9qc"
TypeError
```

---

روش صحیح

```javascript id="n2q7pk"
const numbers = new Set([
    10,
    20,
    30
]);

const result =
    [...numbers]
        .map(number => number * 2);

console.log(result);
```

خروجی

```text id="w4v1tb"
[20, 40, 60]
```

---

استفاده از `filter()`

```javascript id="c7m3ra"
const numbers = new Set([
    10,
    20,
    30,
    40
]);

const result =
    [...numbers]
        .filter(
            number => number >= 30
        );

console.log(result);
```

خروجی

```text id="y8q6pk"
[30, 40]
```

---

# بازگرداندن نتیجه به Set

اگر نتیجه نیز باید یک Set باشد

```javascript id="t1m9qx"
const numbers = new Set([
    10,
    20,
    30
]);

const doubled =
    new Set(

        [...numbers]
            .map(
                number => number * 2
            )

    );

console.log(doubled);
```

خروجی

```text id="b5r2mc"
Set(3) {
    20,
    40,
    60
}
```

---

# ترکیب دو Set

```javascript id="h9q4tp"
const set1 = new Set([
    1,
    2,
    3
]);

const set2 = new Set([
    3,
    4,
    5
]);

const merged =
    new Set([
        ...set1,
        ...set2
    ]);

console.log(merged);
```

خروجی

```text id="v6m8ra"
Set(5) {
    1,
    2,
    3,
    4,
    5
}
```

---

# مثال واقعی

حذف ایمیل‌های تکراری

```javascript id="q2v7mk"
const emails = [
    "ali@test.com",
    "sara@test.com",
    "ali@test.com",
    "reza@test.com"
];

const uniqueEmails =
    [...new Set(emails)];

console.log(uniqueEmails);
```

---

# مثال واقعی

لیست تگ‌های مقاله

```javascript id="f5m3pb"
const tags = [
    "Node.js",
    "JavaScript",
    "Backend",
    "Node.js",
    "API"
];

const uniqueTags =
    new Set(tags);

for (const tag of uniqueTags) {
    console.log(tag);
}
```

---

# مثال واقعی

کاربران آنلاین

```javascript id="m1q8tv"
const onlineUsers =
    new Set();

onlineUsers.add("Ali");
onlineUsers.add("Sara");
onlineUsers.add("Ali");

console.log(
    onlineUsers.size
);
```

خروجی

```text id="x4p6rc"
2
```

---

# خلاصه

| عملیات                | روش                             |
| --------------------- | ------------------------------- |
| پیمایش                | `for...of`                      |
| Set → Array           | `[...set]` یا `Array.from(set)` |
| Array → Set           | `new Set(array)`                |
| حذف داده‌های تکراری   | `[...new Set(array)]`           |
| String → Set          | `new Set(string)`               |
| Set → String          | `[...set].join("")`             |
| استفاده از `map()`    | `[...set].map()`                |
| استفاده از `filter()` | `[...set].filter()`             |

---

# جمع‌بندی بخش سوم

در این بخش یاد گرفتید:

* چگونه با `for...of` روی اعضای Set پیمایش کنید.
* چگونه Set را به Array تبدیل کنید.
* چگونه Array را به Set تبدیل کنید.
* چگونه با استفاده از Set مقادیر تکراری را حذف کنید.
* چگونه با String و Set کار کنید.
* چگونه از متدهای Array مانند `map()` و `filter()` روی داده‌های Set استفاده کنید.
* چگونه نتیجه عملیات را دوباره به یک Set تبدیل کنید.

در بخش بعدی با عملیات مجموعه‌ها (Union، Intersection، Difference و Symmetric Difference) و کاربردهای آن‌ها در پروژه‌های واقعی Node.js آشنا خواهید شد.

---

# بخش ۴

# عملیات روی Set

یکی از مهم‌ترین کاربردهای Set انجام عملیات مجموعه‌ها (Set Operations) است.

این عملیات در ریاضیات، پایگاه داده، الگوریتم‌ها و پروژه‌های واقعی بسیار استفاده می‌شوند.

مهم‌ترین عملیات عبارت‌اند از:

* Union
* Intersection
* Difference
* Symmetric Difference
* Subset
* Superset

در JavaScript این عملیات به صورت متد داخلی وجود ندارند، اما با استفاده از قابلیت‌های Set به سادگی قابل پیاده‌سازی هستند.

---

# Union

## اجتماع دو مجموعه

Union تمام اعضای هر دو مجموعه را برمی‌گرداند و مقادیر تکراری را حذف می‌کند.

---

### مثال

```javascript id="r8m2pa"
const set1 = new Set([
    1,
    2,
    3
]);

const set2 = new Set([
    3,
    4,
    5
]);

const union = new Set([
    ...set1,
    ...set2
]);

console.log(union);
```

خروجی

```text id="v7q5mk"
Set(5) {
    1,
    2,
    3,
    4,
    5
}
```

---

## مثال دیگر

```javascript id="x3p8tb"
const fruits1 = new Set([
    "Apple",
    "Banana"
]);

const fruits2 = new Set([
    "Banana",
    "Orange"
]);

const fruits = new Set([
    ...fruits1,
    ...fruits2
]);

console.log(fruits);
```

خروجی

```text id="g1m6rc"
Set(3) {
    "Apple",
    "Banana",
    "Orange"
}
```

---

# Intersection

## اشتراک دو مجموعه

Intersection فقط اعضایی را برمی‌گرداند که در هر دو Set وجود دارند.

---

### مثال

```javascript id="u5q2pv"
const set1 = new Set([
    1,
    2,
    3,
    4
]);

const set2 = new Set([
    3,
    4,
    5,
    6
]);

const intersection = new Set(

    [...set1].filter(
        value => set2.has(value)
    )

);

console.log(intersection);
```

خروجی

```text id="n8m4qa"
Set(2) {
    3,
    4
}
```

---

## مثال رشته‌ای

```javascript id="d4r7mk"
const roles1 = new Set([
    "read",
    "write",
    "delete"
]);

const roles2 = new Set([
    "write",
    "delete",
    "admin"
]);

const commonRoles = new Set(

    [...roles1].filter(
        role => roles2.has(role)
    )

);

console.log(commonRoles);
```

---

# Difference

## تفاضل دو مجموعه

Difference اعضایی را برمی‌گرداند که فقط در Set اول وجود دارند.

---

### مثال

```javascript id="w9p3tb"
const set1 = new Set([
    1,
    2,
    3,
    4
]);

const set2 = new Set([
    3,
    4
]);

const difference = new Set(

    [...set1].filter(
        value => !set2.has(value)
    )

);

console.log(difference);
```

خروجی

```text id="h2m8rv"
Set(2) {
    1,
    2
}
```

---

# Symmetric Difference

## تفاضل متقارن

اعضایی که فقط در یکی از دو Set وجود دارند.

اعضای مشترک حذف می‌شوند.

---

### مثال

```javascript id="f8q1mc"
const set1 = new Set([
    1,
    2,
    3
]);

const set2 = new Set([
    3,
    4,
    5
]);

const symmetricDifference = new Set([

    ...[...set1].filter(
        value => !set2.has(value)
    ),

    ...[...set2].filter(
        value => !set1.has(value)
    )

]);

console.log(symmetricDifference);
```

خروجی

```text id="y6p4ra"
Set(4) {
    1,
    2,
    4,
    5
}
```

---

# Subset

## بررسی زیرمجموعه بودن

بررسی می‌کند آیا تمام اعضای یک Set داخل Set دیگر وجود دارند یا خیر.

---

### مثال

```javascript id="k5m9tx"
const set1 = new Set([
    1,
    2
]);

const set2 = new Set([
    1,
    2,
    3,
    4
]);

const isSubset =
    [...set1].every(
        value => set2.has(value)
    );

console.log(isSubset);
```

خروجی

```text id="c7q2pk"
true
```

---

مثال دیگر

```javascript id="t3m6rb"
const set1 = new Set([
    1,
    5
]);

const set2 = new Set([
    1,
    2,
    3,
    4
]);

const isSubset =
    [...set1].every(
        value => set2.has(value)
    );

console.log(isSubset);
```

خروجی

```text id="m1v8qc"
false
```

---

# Superset

## بررسی ابرمجموعه بودن

Superset دقیقاً برعکس Subset است.

اگر Set اول تمام اعضای Set دوم را داشته باشد، Superset محسوب می‌شود.

---

### مثال

```javascript id="b4q7tm"
const set1 = new Set([
    1,
    2,
    3,
    4
]);

const set2 = new Set([
    2,
    3
]);

const isSuperset =
    [...set2].every(
        value => set1.has(value)
    );

console.log(isSuperset);
```

خروجی

```text id="q8m5pa"
true
```

---

# مقایسه دو Set

گاهی لازم است بررسی کنیم دو Set دقیقاً برابر هستند یا خیر.

```javascript id="n2r9kv"
const set1 = new Set([
    1,
    2,
    3
]);

const set2 = new Set([
    1,
    2,
    3
]);

const isEqual =

    set1.size === set2.size &&

    [...set1].every(
        value => set2.has(value)
    );

console.log(isEqual);
```

خروجی

```text id="p6v3rc"
true
```

---

# حذف اعضای تکراری چند Array

```javascript id="j7m4tb"
const numbers1 = [
    1,
    2,
    3
];

const numbers2 = [
    3,
    4,
    5
];

const numbers3 = [
    5,
    6,
    7
];

const result = new Set([
    ...numbers1,
    ...numbers2,
    ...numbers3
]);

console.log(result);
```

---

# مثال واقعی

## کاربران آنلاین

```javascript id="u8q1pm"
const server1 = new Set([
    "Ali",
    "Sara"
]);

const server2 = new Set([
    "Sara",
    "Reza"
]);

const onlineUsers = new Set([
    ...server1,
    ...server2
]);

console.log(onlineUsers);
```

---

# مثال واقعی

## کاربران مشترک دو گروه

```javascript id="x5m7ra"
const groupA = new Set([
    "Ali",
    "Sara",
    "Reza"
]);

const groupB = new Set([
    "Sara",
    "Reza",
    "Amir"
]);

const commonUsers = new Set(

    [...groupA].filter(
        user => groupB.has(user)
    )

);

console.log(commonUsers);
```

---

# مثال واقعی

## مجوزهای حذف شده

```javascript id="e9p2tv"
const oldPermissions = new Set([
    "read",
    "write",
    "delete"
]);

const newPermissions = new Set([
    "read",
    "write"
]);

const removed = new Set(

    [...oldPermissions].filter(
        permission =>
            !newPermissions.has(permission)
    )

);

console.log(removed);
```

---

# مثال واقعی

## نقش‌های جدید

```javascript id="v6m4pk"
const oldRoles = new Set([
    "user"
]);

const newRoles = new Set([
    "user",
    "editor",
    "admin"
]);

const addedRoles = new Set(

    [...newRoles].filter(
        role => !oldRoles.has(role)
    )

);

console.log(addedRoles);
```

---

# جدول خلاصه

| عملیات               | توضیح                                    |
| -------------------- | ---------------------------------------- |
| Union                | ترکیب دو Set بدون عضو تکراری             |
| Intersection         | اعضای مشترک دو Set                       |
| Difference           | اعضای موجود در Set اول و غایب در Set دوم |
| Symmetric Difference | اعضای غیرمشترک دو Set                    |
| Subset               | بررسی زیرمجموعه بودن                     |
| Superset             | بررسی ابرمجموعه بودن                     |
| Equality             | بررسی برابری دو Set                      |

---

# کاربردهای واقعی Set Operations

این عملیات در پروژه‌های واقعی بسیار کاربرد دارند.

نمونه‌هایی از کاربردها:

* حذف کاربران تکراری
* بررسی مجوزهای کاربران
* مقایسه نقش‌های کاربران
* یافتن محصولات مشترک بین دو فروشگاه
* مقایسه تگ‌های دو مقاله
* تحلیل داده‌های آماری
* همگام‌سازی اطلاعات بین دو منبع داده
* مدیریت کش (Cache)
* سیستم پیشنهاد دوستان
* مدیریت لیست علاقه‌مندی‌ها

---

# جمع‌بندی بخش چهارم

در این بخش با مهم‌ترین عملیات روی Set آشنا شدید.

اکنون می‌توانید:

* دو Set را با هم ترکیب کنید (Union).
* اعضای مشترک دو Set را پیدا کنید (Intersection).
* اعضای متفاوت را استخراج کنید (Difference).
* تفاضل متقارن دو Set را به دست آورید (Symmetric Difference).
* زیرمجموعه یا ابرمجموعه بودن Setها را بررسی کنید.
* برابری دو Set را تشخیص دهید.
* از این عملیات در سناریوهای واقعی Node.js مانند مدیریت کاربران، نقش‌ها، مجوزها و داده‌های یکتا استفاده کنید.

---

# بخش ۵

# اشتباهات رایج

در این بخش با رایج‌ترین اشتباهاتی آشنا می‌شوید که برنامه‌نویسان هنگام کار با Set مرتکب می‌شوند.

شناخت این اشتباهات باعث می‌شود کدهای مطمئن‌تر و حرفه‌ای‌تری بنویسید.

---

# اشتباه ۱

## استفاده از اندیس

بعضی افراد تصور می‌کنند Set مانند Array دارای اندیس است.

اشتباه

```javascript
const numbers = new Set([
    10,
    20,
    30
]);

console.log(numbers[0]);
```

خروجی

```text
undefined
```

دلیل

Set اندیس ندارد.

روش صحیح

```javascript
for (const number of numbers) {
    console.log(number);
}
```

---

# اشتباه ۲

## استفاده از length

اشتباه

```javascript
const numbers = new Set([
    10,
    20,
    30
]);

console.log(numbers.length);
```

خروجی

```text
undefined
```

دلیل

Set دارای ویژگی `length` نیست.

روش صحیح

```javascript
console.log(
    numbers.size
);
```

---

# اشتباه ۳

## استفاده از map()

اشتباه

```javascript
const numbers = new Set([
    1,
    2,
    3
]);

numbers.map(
    number => number * 2
);
```

خروجی

```text
TypeError
```

دلیل

Set متد `map()` ندارد.

روش صحیح

```javascript
const result =
    [...numbers].map(
        number => number * 2
    );

console.log(result);
```

---

# اشتباه ۴

## استفاده از filter()

اشتباه

```javascript
numbers.filter(
    number => number > 10
);
```

روش صحیح

```javascript
const result =
    [...numbers].filter(
        number => number > 10
    );
```

---

# اشتباه ۵

## تصور حذف Objectهای مشابه

```javascript
const users = new Set();

users.add({
    name: "Ali"
});

users.add({
    name: "Ali"
});

console.log(users.size);
```

خروجی

```text
2
```

دلیل

هر Object یک Reference مستقل دارد.

Set محتوا را مقایسه نمی‌کند.

بلکه Reference را مقایسه می‌کند.

---

# اشتباه ۶

## تصور مرتب بودن اعضا

بعضی افراد تصور می‌کنند Set مانند `sort()` اعضا را مرتب می‌کند.

```javascript
const numbers = new Set([
    30,
    10,
    20
]);

console.log(numbers);
```

خروجی

```text
Set(3) {
    30,
    10,
    20
}
```

Set ترتیب ورود اعضا را حفظ می‌کند.

نه ترتیب عددی یا الفبایی.

---

# اشتباه ۷

## استفاده از delete با اندیس

اشتباه

```javascript
numbers.delete(0);
```

بعضی تصور می‌کنند عدد صفر یعنی اولین عضو.

در واقع متد `delete()` مقدار را حذف می‌کند.

روش صحیح

```javascript
numbers.delete(10);
```

---

# اشتباه ۸

## فراموش کردن مقدار بازگشتی has()

```javascript
numbers.has(20);
```

این کد هیچ خروجی‌ای تولید نمی‌کند.

باید نتیجه را استفاده کنید.

```javascript
if (
    numbers.has(20)
) {

    console.log("Exists");

}
```

---

# اشتباه ۹

## استفاده از مقدار تکراری و انتظار افزایش size

```javascript
const numbers = new Set();

numbers.add(10);

numbers.add(10);

numbers.add(10);

console.log(numbers.size);
```

خروجی

```text
1
```

دلیل

Set مقدار تکراری ذخیره نمی‌کند.

---

# اشتباه ۱۰

## استفاده از push()

اشتباه

```javascript
numbers.push(50);
```

خروجی

```text
TypeError
```

روش صحیح

```javascript
numbers.add(50);
```

---

# اشتباه ۱۱

## استفاده از pop()

اشتباه

```javascript
numbers.pop();
```

Set متدی برای حذف آخرین عضو ندارد.

---

# اشتباه ۱۲

## استفاده از includes()

اشتباه

```javascript
numbers.includes(20);
```

روش صحیح

```javascript
numbers.has(20);
```

---

# اشتباه ۱۳

## استفاده مستقیم از متدهای Array

اشتباه

```javascript
numbers.reverse();

numbers.sort();

numbers.flat();

numbers.slice();
```

این متدها متعلق به Array هستند.

---

# اشتباه ۱۴

## استفاده نادرست از clear()

```javascript
const result =
    numbers.clear();

console.log(result);
```

خروجی

```text
undefined
```

`clear()` چیزی برنمی‌گرداند.

فقط تمام اعضا را حذف می‌کند.

---

# اشتباه ۱۵

## مقایسه دو Set با ===

```javascript
const set1 = new Set([
    1,
    2
]);

const set2 = new Set([
    1,
    2
]);

console.log(
    set1 === set2
);
```

خروجی

```text
false
```

دلیل

هر Set یک Reference جدید است.

---

روش صحیح

```javascript
const equal =

    set1.size === set2.size &&

    [...set1].every(
        value => set2.has(value)
    );

console.log(equal);
```

---

# اشتباه ۱۶

## تغییر مستقیم هنگام پیمایش

```javascript
const numbers = new Set([
    1,
    2,
    3
]);

for (const number of numbers) {

    numbers.delete(number);

}
```

اگرچه این کار در بسیاری از موارد اجرا می‌شود، اما تغییر مجموعه هنگام پیمایش می‌تواند باعث ایجاد رفتارهای غیرمنتظره و کاهش خوانایی کد شود.

روش بهتر

```javascript
const numbers = new Set([
    1,
    2,
    3
]);

const copy =
    [...numbers];

for (const number of copy) {

    numbers.delete(number);

}
```

---

# اشتباه ۱۷

## استفاده از Set برای داده‌های دارای کلید

بعضی افراد اطلاعاتی مانند زیر را داخل Set قرار می‌دهند.

```javascript
const users = new Set([
    {
        id: 1,
        name: "Ali"
    }
]);
```

اگر هدف دسترسی بر اساس کلید باشد، `Map` انتخاب مناسب‌تری است.

Set فقط برای نگهداری مجموعه‌ای از مقادیر یکتا طراحی شده است.

---

# اشتباه ۱۸

## فراموش کردن تبدیل Set به Array

```javascript
const numbers = new Set([
    1,
    2,
    3
]);

const json =
    JSON.stringify(numbers);

console.log(json);
```

خروجی

```text
{}
```

زیرا `Set` به صورت مستقیم به JSON تبدیل نمی‌شود.

روش صحیح

```javascript
const json =
    JSON.stringify(
        [...numbers]
    );

console.log(json);
```

---

# اشتباه ۱۹

## استفاده از Array زمانی که Set مناسب‌تر است

```javascript
const users = [];

users.push("Ali");
users.push("Ali");
users.push("Ali");
```

اگر داده نباید تکراری باشد، استفاده از Set انتخاب بهتری است.

```javascript
const users = new Set();

users.add("Ali");
users.add("Ali");
users.add("Ali");

console.log(users.size);
```

---

# اشتباه ۲۰

## انتخاب نادرست بین Set و Object

اگر داده‌ها به صورت کلید و مقدار هستند، از Set استفاده نکنید.

اشتباه

```javascript
const user = new Set();
```

برای چنین داده‌هایی بهتر است از Object یا Map استفاده شود.

```javascript
const user = {
    name: "Ali",
    age: 22
};
```

---

# خلاصه اشتباهات رایج

| اشتباه                       | روش صحیح                                 |
| ---------------------------- | ---------------------------------------- |
| استفاده از `length`          | استفاده از `size`                        |
| استفاده از اندیس             | پیمایش با `for...of`                     |
| استفاده از `push()`          | استفاده از `add()`                       |
| استفاده از `pop()`           | استفاده از `delete()` یا ایجاد Set جدید  |
| استفاده از `includes()`      | استفاده از `has()`                       |
| استفاده از `map()` مستقیم    | تبدیل به Array و سپس `map()`             |
| استفاده از `filter()` مستقیم | تبدیل به Array و سپس `filter()`          |
| مقایسه با `===`              | مقایسه اعضا و `size`                     |
| انتظار حذف Objectهای مشابه   | توجه به مقایسه بر اساس Reference         |
| تبدیل مستقیم به JSON         | تبدیل به Array قبل از `JSON.stringify()` |

---

# جمع‌بندی بخش پنجم

در این بخش با رایج‌ترین اشتباهات هنگام کار با Set آشنا شدید.

اکنون می‌دانید:

* چرا Set اندیس ندارد.
* تفاوت `size` و `length` چیست.
* چرا متدهای Array روی Set کار نمی‌کنند.
* چگونه Setها را به‌درستی مقایسه کنید.
* چگونه از مشکلات مربوط به Reference جلوگیری کنید.
* چه زمانی از Set، Array، Object یا Map استفاده کنید.

شناخت این اشتباهات باعث می‌شود هنگام استفاده از Set در پروژه‌های واقعی Node.js کدهای پایدارتر، خواناتر و قابل نگهداری‌تری بنویسید.

---

