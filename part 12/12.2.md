# فصل 96 - SQL

## مقدمه

پایگاه داده یکی از مهم‌ترین بخش‌های هر برنامه Backend است. تقریباً تمام برنامه‌های واقعی نیاز دارند اطلاعات کاربران، محصولات، سفارش‌ها، تراکنش‌ها و داده‌های مختلف را ذخیره، مدیریت و بازیابی کنند.

SQL یکی از اصلی‌ترین زبان‌هایی است که برای کار با پایگاه‌های داده رابطه‌ای (Relational Database) استفاده می‌شود. یادگیری SQL برای هر برنامه‌نویس Node.js که قصد ورود به بازار کار Backend را دارد ضروری است؛ زیرا بسیاری از پروژه‌های واقعی از دیتابیس‌های SQL مانند PostgreSQL و MySQL استفاده می‌کنند.

در این فصل ابتدا با مفهوم دیتابیس‌های رابطه‌ای آشنا می‌شویم، سپس SQL را از پایه یاد می‌گیریم و در ادامه به Query نویسی حرفه‌ای، طراحی جدول، ارتباط بین داده‌ها، بهینه‌سازی Queryها و استفاده از SQL در پروژه‌های Node.js می‌پردازیم.

هدف این فصل این است که بتوانید بدون وابستگی به ابزار خاص، منطق کار با دیتابیس‌های SQL را یاد بگیرید و آماده استفاده از آن در پروژه‌های واقعی Node.js شوید.

---

# بخش ۱ - آشنایی با Database و SQL

## 96.1 - Database چیست؟

* مفهوم Database
* چرا به دیتابیس نیاز داریم؟
* ذخیره اطلاعات بدون دیتابیس
* مشکلات ذخیره‌سازی سنتی
* انواع Database

تمرین

---

## 96.2 - انواع Database

* Relational Database
* NoSQL Database
* تفاوت SQL و NoSQL
* چه زمانی از SQL استفاده کنیم؟
* چه زمانی از NoSQL استفاده کنیم؟

تمرین

---

## 96.3 - SQL چیست؟

* تعریف SQL
* تاریخچه SQL
* کاربردهای SQL
* SQL Standard
* Database Engine چیست؟

تمرین

---

# بخش ۲ - مفاهیم دیتابیس رابطه‌ای

## 96.4 - Relational Database

* مفهوم Relation
* Table
* Row
* Column
* Record
* Field

تمرین

---

## 96.5 - طراحی جدول

* انتخاب Columnها
* Data Type
* Primary Key
* Unique Value
* Default Value
* Nullable

تمرین

---

## 96.6 - Data Types در SQL

* Integer
* Decimal
* Float
* Boolean
* Char
* Varchar
* Text
* Date
* Time
* Timestamp
* JSON

تمرین

---

# بخش ۳ - نصب و آماده‌سازی محیط SQL

## 96.7 - نصب Database Server

* PostgreSQL
* MySQL
* SQLite

---

## 96.8 - ابزارهای مدیریت Database

* Command Line Client
* Database GUI
* Query Editor

معرفی:

* pgAdmin
* DBeaver
* TablePlus

تمرین

---

# بخش ۴ - دستورات پایه SQL

## 96.9 - ساخت Database

* CREATE DATABASE
* حذف Database
* انتخاب Database

تمرین

---

## 96.10 - ساخت Table

* CREATE TABLE
* تعریف Column
* تعیین Data Type
* تعیین Constraint

تمرین

---

## 96.11 - تغییر ساختار Table

* ALTER TABLE
* ADD COLUMN
* DROP COLUMN
* MODIFY COLUMN
* Rename Column

تمرین

---

## 96.12 - حذف داده و جدول

* DROP
* DELETE
* TRUNCATE

تفاوت‌ها

تمرین

---

# بخش ۵ - CRUD در SQL

## 96.13 - Insert کردن داده

* INSERT INTO
* Insert چند رکورد
* Insert با Select

تمرین

---

## 96.14 - خواندن داده‌ها

* SELECT
* انتخاب Columnها
* Alias
* DISTINCT

تمرین

---

## 96.15 - فیلتر کردن داده‌ها

* WHERE
* AND
* OR
* NOT
* BETWEEN
* IN
* LIKE

تمرین

---

## 96.16 - مرتب‌سازی داده‌ها

* ORDER BY
* ASC
* DESC

تمرین

---

## 96.17 - محدود کردن خروجی

* LIMIT
* OFFSET
* Pagination

تمرین

---

## 96.18 - بروزرسانی داده‌ها

* UPDATE
* تغییر چند مقدار
* شرط در Update

تمرین

---

# بخش ۶ - Query نویسی حرفه‌ای

## 96.19 - توابع SQL

* COUNT
* SUM
* AVG
* MIN
* MAX

تمرین

---

## 96.20 - GROUP BY

* گروه‌بندی داده‌ها
* Aggregate Function
* HAVING

تمرین

---

## 96.21 - JOIN در SQL

* مفهوم Join
* INNER JOIN
* LEFT JOIN
* RIGHT JOIN
* FULL JOIN

تمرین

---

## 96.22 - ارتباط بین جدول‌ها

* One To One
* One To Many
* Many To Many

تمرین

---

## 96.23 - Foreign Key

* مفهوم Foreign Key
* Constraint
* Cascade
* Restrict

تمرین

---

# بخش ۷ - مفاهیم پیشرفته SQL

## 96.24 - Index

* Index چیست؟
* چرا Index استفاده می‌کنیم؟
* ایجاد Index
* حذف Index
* مزایا و معایب

تمرین

---

## 96.25 - Transaction

* مفهوم Transaction
* ACID
* COMMIT
* ROLLBACK

تمرین

---

## 96.26 - View

* View چیست؟
* ساخت View
* کاربرد View

تمرین

---

## 96.27 - Stored Procedure

* مفهوم Procedure
* Function در Database
* کاربردها

تمرین

---

## 96.28 - Trigger

* Trigger چیست؟
* Before Trigger
* After Trigger
* کاربردهای واقعی

تمرین

---

# بخش ۸ - امنیت در SQL

## 96.29 - SQL Injection

* SQL Injection چیست؟
* روش‌های حمله
* مثال عملی
* جلوگیری از Injection

تمرین

---

## 96.30 - Prepared Statement

* مفهوم Prepared Query
* Parameter Binding
* امنیت Queryها

تمرین

---

# بخش ۹ - SQL در Node.js

## 96.31 - اتصال Node.js به SQL Database

* Driver چیست؟
* نصب Package دیتابیس
* ساخت Connection

تمرین

---

## 96.32 - اجرای Query در Node.js

* ارسال Query
* دریافت Result
* مدیریت Error

تمرین

---

## 96.33 - Connection Pool

* Pool چیست؟
* چرا استفاده می‌کنیم؟
* مدیریت Connectionها

تمرین

---

## 96.34 - ساخت API با SQL

پروژه:

سیستم مدیریت کاربران

شامل:

* ثبت کاربر
* دریافت کاربران
* حذف کاربر
* ویرایش کاربر

---

# بخش ۱۰ - اشتباهات رایج

# بخش ۱۱ - نکات حرفه‌ای


# بخش ۱۲ - تمرین‌ها


# بخش ۱۳ - پاسخ تشریحی تمرین‌ها


# بخش ۱۴ - مینی پروژه‌ها

---

# بخش ۱ - آشنایی با Database و SQL

# 96.1 - Database چیست؟

## Database چیست؟

در هر نرم‌افزاری که ساخته می‌شود، معمولاً نیاز داریم اطلاعاتی را ذخیره کنیم.

برای مثال:

* اطلاعات کاربران
* محصولات یک فروشگاه
* پیام‌های یک پیام‌رسان
* سفارش‌های مشتریان
* اطلاعات حساب‌های بانکی
* تنظیمات برنامه
* گزارش‌ها و لاگ‌ها

اگر این اطلاعات فقط در حافظه برنامه نگهداری شوند، با بسته شدن برنامه یا خاموش شدن سیستم از بین می‌روند.

برای حل این مشکل از **Database** استفاده می‌کنیم.

Database یا پایگاه داده، سیستمی برای **ذخیره‌سازی، مدیریت، سازمان‌دهی و بازیابی اطلاعات** است.

به زبان ساده:

> Database یک محل ذخیره‌سازی هوشمند است که برنامه‌ها می‌توانند اطلاعات خود را در آن ذخیره کنند و هر زمان لازم بود آن‌ها را دریافت یا تغییر دهند.

---

## چرا به Database نیاز داریم؟

فرض کنید یک فروشگاه اینترنتی داریم.

بدون Database باید اطلاعاتی مانند:

```text
نام کاربر:
Sobhan

ایمیل:
sobhan@example.com

محصول خریداری شده:
Laptop

تاریخ خرید:
2026-08-07
```

را داخل فایل‌ها ذخیره کنیم.

اما زمانی که تعداد کاربران زیاد شود:

* پیدا کردن اطلاعات سخت می‌شود.
* تغییر اطلاعات زمان زیادی می‌گیرد.
* امنیت کاهش پیدا می‌کند.
* مدیریت داده‌ها پیچیده می‌شود.

Database این مشکلات را حل می‌کند.

---

## کاربردهای Database در دنیای واقعی

تقریباً تمام نرم‌افزارهای بزرگ از Database استفاده می‌کنند.

مثال:

### شبکه‌های اجتماعی

مانند:

* کاربران
* پست‌ها
* کامنت‌ها
* لایک‌ها
* پیام‌ها

در Database ذخیره می‌شوند.

---

### فروشگاه‌های اینترنتی

اطلاعات:

* محصولات
* کاربران
* سفارش‌ها
* پرداخت‌ها
* موجودی انبار

در Database قرار می‌گیرند.

---

### سیستم‌های بانکی

اطلاعات:

* حساب‌ها
* تراکنش‌ها
* موجودی
* کاربران

نیاز به ذخیره‌سازی امن دارند.

---

### برنامه‌های Node.js

در پروژه‌های Backend با Node.js معمولاً Database برای ذخیره اطلاعات استفاده می‌شود.

مثلاً:

```text
Client
 |
 |
Node.js Server
 |
 |
Database
```

کاربر درخواست ارسال می‌کند.

سرور Node.js اطلاعات مورد نیاز را از Database دریافت می‌کند یا اطلاعات جدیدی داخل آن ذخیره می‌کند.

---

# ذخیره اطلاعات بدون Database

قبل از استفاده گسترده از Database، برنامه‌ها معمولاً اطلاعات را در فایل ذخیره می‌کردند.

مثلاً:

```
users.txt
```

محتوا:

```text
1,Sobhan,sobhan@test.com
2,Ali,ali@test.com
3,Reza,reza@test.com
```

یا:

```
users.json
```

```json
[
  {
    "id":1,
    "name":"Sobhan"
  },
  {
    "id":2,
    "name":"Ali"
  }
]
```

برای پروژه‌های کوچک این روش ممکن است قابل استفاده باشد.

اما در پروژه‌های واقعی مشکلات زیادی ایجاد می‌کند.

---

# مشکلات ذخیره‌سازی سنتی

## 1. سرعت پایین جستجو

فرض کنید یک فایل با میلیون‌ها کاربر داریم.

برای پیدا کردن یک کاربر باید کل فایل بررسی شود.

Database برای این کار ابزارهای مخصوص دارد.

---

## 2. مشکل همزمانی کاربران

فرض کنید دو کاربر همزمان اطلاعات خود را تغییر دهند.

در ذخیره‌سازی فایل ممکن است اطلاعات یکی از کاربران حذف شود.

Database این مشکل را مدیریت می‌کند.

---

## 3. امنیت پایین

فایل‌های ساده معمولاً:

* کنترل دسترسی ندارند.
* سطح‌بندی کاربران ندارند.
* امکانات امنیتی محدود دارند.

---

## 4. نبود ساختار مناسب

وقتی داده‌ها زیاد شوند، مدیریت فایل‌ها سخت می‌شود.

مثلاً:

```
users.json

products.json

orders.json

payments.json
```

ارتباط بین این فایل‌ها پیچیده خواهد شد.

---

## 5. نبود امکانات حرفه‌ای

Database امکاناتی مانند:

* جستجوی پیشرفته
* مرتب‌سازی
* فیلتر کردن
* تراکنش
* Backup
* Indexing

را ارائه می‌دهد.

---

# Database چگونه کار می‌کند؟

Database معمولاً از چند بخش تشکیل شده است.

```
Application

      |
      |

Database Engine

      |
      |

Storage
```

---

## Application

برنامه‌ای است که ما می‌نویسیم.

مثلاً:

* Node.js
* Java
* Python
* PHP

---

## Database Engine

بخشی است که درخواست‌های ما را پردازش می‌کند.

مثلاً:

* ذخیره اطلاعات
* خواندن اطلاعات
* تغییر اطلاعات
* حذف اطلاعات

---

## Storage

محلی است که اطلاعات واقعی روی دیسک ذخیره می‌شوند.

---

# انواع Database

Databaseها مدل‌های مختلفی دارند.

مهم‌ترین دسته‌بندی:

## 1. Relational Database

یا:

## پایگاه داده رابطه‌ای

اطلاعات را در جدول‌ها ذخیره می‌کند.

مثال:

```
Users Table

+----+---------+----------------+
| id | name    | email          |
+----+---------+----------------+
| 1  | Ali     | ali@test.com   |
| 2  | Reza    | reza@test.com  |
+----+---------+----------------+
```

نمونه‌ها:

* PostgreSQL
* MySQL
* SQL Server
* Oracle

---

## 2. NoSQL Database

برای ذخیره اطلاعات غیرجدولی استفاده می‌شود.

مثلاً:

```json
{
 "name":"Sobhan",
 "age":22,
 "skills":[
   "Node.js",
   "JavaScript"
 ]
}
```

نمونه‌ها:

* MongoDB
* Redis
* Cassandra

---

# تمرین 96.1

## تمرین 1

Database چیست؟

---

## تمرین 2

سه دلیل برای استفاده از Database بنویسید.

---

## تمرین 3

فرض کنید می‌خواهید یک سیستم مدیریت کتابخانه بسازید.

چه اطلاعاتی باید ذخیره شوند؟

---

## تمرین 4

ذخیره اطلاعات کاربران در یک فایل JSON چه مشکلاتی می‌تواند داشته باشد؟

---

# پاسخ تمرین 96.1

## پاسخ 1

Database سیستمی برای ذخیره، مدیریت و بازیابی اطلاعات است.

---

## پاسخ 2

سه دلیل:

* مدیریت بهتر اطلاعات
* جستجوی سریع‌تر
* امنیت بیشتر

---

## پاسخ 3

اطلاعاتی مانند:

* نام کتاب
* نویسنده
* اعضای کتابخانه
* تاریخ امانت
* وضعیت کتاب

---

## پاسخ 4

مشکلات:

* سرعت پایین
* سخت شدن مدیریت داده‌ها
* مشکلات همزمانی
* امنیت پایین

---

---

# 96.2 - انواع Database

## Relational Database چیست؟

Relational Database یا Database رابطه‌ای، اطلاعات را در قالب جدول ذخیره می‌کند.

هر جدول شامل:

* ستون‌ها (Column)
* ردیف‌ها (Row)

است.

مثال:

جدول کاربران:

| id | name | email                                 |
| -- | ---- | ------------------------------------- |
| 1  | Ali  | [ali@test.com](mailto:ali@test.com)   |
| 2  | Sara | [sara@test.com](mailto:sara@test.com) |

---

## ویژگی‌های Database رابطه‌ای

### ساختار مشخص

قبل از ذخیره اطلاعات باید ساختار جدول مشخص شود.

مثلاً:

```
Users

id
name
email
password
```

---

### استفاده از SQL

برای کار با این Databaseها از زبان SQL استفاده می‌کنیم.

مثلاً:

```sql
SELECT *
FROM users;
```

---

### ارتباط بین داده‌ها

می‌توان بین جدول‌ها ارتباط ایجاد کرد.

مثلاً:

```
Users

     |
     |

Orders
```

یک کاربر می‌تواند چند سفارش داشته باشد.

---

# NoSQL Database چیست؟

NoSQL یعنی:

Not Only SQL

این Databaseها برای داده‌هایی طراحی شده‌اند که ساختار ثابت ندارند.

مثلاً:

```json
{
 "username":"sobhan",
 "skills":[
   "Node.js",
   "React"
 ]
}
```

---

## ویژگی‌های NoSQL

* انعطاف‌پذیری بالا
* مناسب داده‌های بزرگ
* مقیاس‌پذیری راحت‌تر
* مناسب پروژه‌های Real-Time

---

# تفاوت SQL و NoSQL

| SQL                     | NoSQL                      |
| ----------------------- | -------------------------- |
| جدولی                   | سندی یا مدل‌های دیگر       |
| ساختار ثابت             | ساختار انعطاف‌پذیر         |
| SQL Query               | روش‌های مختلف Query        |
| مناسب داده‌های رابطه‌ای | مناسب داده‌های بزرگ و سریع |

---

# چه زمانی از SQL استفاده کنیم؟

SQL مناسب است وقتی:

* داده‌ها ارتباط زیادی دارند.
* ساختار اطلاعات مشخص است.
* تراکنش مهم است.

مثال:

* بانک
* سیستم حسابداری
* فروشگاه اینترنتی

---

# چه زمانی از NoSQL استفاده کنیم؟

NoSQL مناسب است وقتی:

* داده‌ها دائماً تغییر می‌کنند.
* سرعت بالا مهم است.
* داده‌ها حجم زیادی دارند.

مثال:

* چت آنلاین
* سیستم لاگ
* تحلیل داده

---

# تمرین 96.2

## تمرین 1

تفاوت اصلی SQL و NoSQL چیست؟

---

## تمرین 2

برای سیستم بانکی SQL بهتر است یا NoSQL؟ چرا؟

---

## تمرین 3

برای ذخیره پیام‌های یک پیام‌رسان چه Databaseای پیشنهاد می‌کنید؟

---

# پاسخ تمرین 96.2

## پاسخ 1

SQL معمولاً جدول‌محور و دارای ساختار ثابت است، اما NoSQL انعطاف‌پذیرتر است.

---

## پاسخ 2

SQL، زیرا اطلاعات بانکی دارای ارتباط و تراکنش‌های حساس هستند.

---

## پاسخ 3

بسته به معماری پروژه می‌تواند NoSQL مانند MongoDB استفاده شود، زیرا پیام‌ها حجم زیادی دارند و ساختارشان ممکن است متفاوت باشد.

---

---

# 96.3 - SQL چیست؟

## تعریف SQL

SQL مخفف:

```
Structured Query Language
```

است.

SQL زبانی برای ارتباط با Databaseهای رابطه‌ای است.

با SQL می‌توانیم:

* اطلاعات را بخوانیم.
* اطلاعات جدید اضافه کنیم.
* اطلاعات را تغییر دهیم.
* اطلاعات را حذف کنیم.

---

مثال:

خواندن کاربران:

```sql
SELECT *
FROM users;
```

اضافه کردن کاربر:

```sql
INSERT INTO users(name)
VALUES('Sobhan');
```

---

# تاریخچه SQL

SQL در دهه 1970 توسط شرکت IBM توسعه پیدا کرد.

هدف آن ایجاد روشی ساده برای کار با Databaseهای رابطه‌ای بود.

بعدها SQL به یک استاندارد جهانی تبدیل شد.

---

# کاربردهای SQL

SQL در موارد زیر استفاده می‌شود:

* Backend Development
* تحلیل داده
* سیستم‌های مالی
* برنامه‌های سازمانی
* گزارش‌گیری
* مدیریت اطلاعات

---

# SQL Standard چیست؟

SQL یک استاندارد دارد که توسط سازمان‌هایی مانند:

ANSI

و

ISO

تعریف شده است.

Databaseهای مختلف ممکن است کمی تفاوت داشته باشند.

مثلاً:

* PostgreSQL
* MySQL
* SQL Server

همگی SQL دارند اما امکانات اضافی متفاوتی ارائه می‌دهند.

---

# Database Engine چیست؟

Database Engine هسته اصلی Database است.

وظیفه آن:

* اجرای Queryها
* مدیریت ذخیره‌سازی
* کنترل دسترسی
* مدیریت تراکنش‌ها

است.

مثال:

در PostgreSQL:

```
PostgreSQL Engine
```

مسئول اجرای دستورات SQL است.

---

# ارتباط Node.js با SQL

در Node.js معمولاً با استفاده از Libraryها به Database وصل می‌شویم.

مثلاً:

```
Node.js

   |

Driver

   |

PostgreSQL
```

کتابخانه‌های معروف:

* pg
* Prisma
* Drizzle

---

# تمرین 96.3

## تمرین 1

SQL مخفف چیست؟

---

## تمرین 2

سه کاری که با SQL انجام می‌دهیم را نام ببرید.

---

## تمرین 3

Database Engine چه وظیفه‌ای دارد؟

---

# پاسخ تمرین 96.3

## پاسخ 1

Structured Query Language

---

## پاسخ 2

* خواندن اطلاعات
* اضافه کردن اطلاعات
* حذف و تغییر اطلاعات

---

## پاسخ 3

Database Engine درخواست‌ها را پردازش می‌کند و عملیات روی Database را انجام می‌دهد.

---

# بخش ۲ - مفاهیم دیتابیس رابطه‌ای

# 96.4 - Relational Database

## Relational Database چیست؟

قبل از کار با SQL باید با مفهوم **Relational Database** آشنا شویم.

Relational Database یا **پایگاه داده رابطه‌ای** نوعی سیستم ذخیره‌سازی اطلاعات است که داده‌ها را در ساختاری منظم و قابل ارتباط با یکدیگر ذخیره می‌کند.

در این مدل، اطلاعات داخل **Table**ها ذخیره می‌شوند و بین جدول‌ها می‌توان رابطه ایجاد کرد.

برای مثال یک فروشگاه اینترنتی را در نظر بگیرید:

```
Users

+----+---------+----------------+
| id | name    | email          |
+----+---------+----------------+
| 1  | Ali     | ali@test.com   |
| 2  | Sara    | sara@test.com  |
+----+---------+----------------+
```

و جدول سفارش‌ها:

```
Orders

+----+---------+--------+
| id | user_id | price  |
+----+---------+--------+
| 1  | 1       | 500    |
| 2  | 2       | 900    |
+----+---------+--------+
```

اینجا با استفاده از `user_id` می‌توانیم سفارش‌ها را به کاربران متصل کنیم.

---

# مفهوم Relation

در پایگاه داده رابطه‌ای، کلمه **Relation** به ساختار جدولی داده‌ها اشاره دارد.

در SQL معمولا هر Relation یک Table است.

مثلا:

```
Users
```

یک Relation محسوب می‌شود.

این Relation شامل:

* ستون‌ها
* ردیف‌ها
* داده‌ها

است.

---

# Table چیست؟

Table یا جدول، اصلی‌ترین بخش Database رابطه‌ای است.

هر جدول مجموعه‌ای از اطلاعات مرتبط را نگهداری می‌کند.

مثلا:

جدول کاربران:

```
Users

id | username | age
--------------------
1  | ali      | 20
2  | sara     | 25
```

هر جدول معمولا یک موضوع مشخص را مدیریت می‌کند.

مثلا:

```
Users
Products
Orders
Payments
Comments
```

---

# Row چیست؟

Row یا ردیف، یک رکورد کامل از اطلاعات است.

هر Row نشان‌دهنده یک موجودیت واقعی است.

مثلا:

```
Users

id | name | age
----------------
1  | Ali  | 20
```

این یک Row است.

یعنی اطلاعات یک کاربر.

---

# Column چیست؟

Column یا ستون، یک ویژگی از داده‌ها را مشخص می‌کند.

مثلا:

```
Users

id | name | email
------------------
1  | Ali  | a@test.com
```

در این جدول:

```
id
name
email
```

ستون هستند.

هر Column معمولا یک نوع داده مشخص دارد.

مثلا:

```
age → Integer

email → Varchar

created_at → Timestamp
```

---

# Record چیست؟

Record همان یک مجموعه کامل از اطلاعات یک موجودیت است.

در SQL معمولا Record همان Row در نظر گرفته می‌شود.

مثلا:

```
id: 1
name: Ali
email: ali@test.com
```

یک Record است.

---

# Field چیست؟

Field یک مقدار داخل یک Record است.

مثلا:

```
User

id | name | age
----------------
1  | Ali  | 20
```

مقدار:

```
Ali
```

یک Field است.

یا:

```
20
```

یک Field دیگر است.

---

# تفاوت Row، Record و Field

مثال:

```
Users

id | name | age
----------------
1  | Ali  | 20
2  | Sara | 25
```

| مفهوم  | مقدار          |
| ------ | -------------- |
| Table  | Users          |
| Row    | کل اطلاعات Ali |
| Record | کل اطلاعات Ali |
| Column | name           |
| Field  | Ali            |

---

# ارتباط مفاهیم

ساختار کلی:

```
Database

 └── Table

      ├── Column
      │
      └── Row
            |
            └── Field
```

مثلا:

```
Database
   |
   └── Users Table
          |
          ├── Columns
          |      ├── id
          |      ├── name
          |      └── email
          |
          └── Rows
                 |
                 └── Record
                        |
                        └── Fields
```

---

# چرا Database رابطه‌ای مهم است؟

مزایای Relational Database:

## ساختار منظم

اطلاعات دارای نظم مشخص هستند.

---

## جلوگیری از تکرار داده

به جای ذخیره چندباره اطلاعات:

```
Ali
Ali
Ali
```

می‌توانیم یک کاربر بسازیم و با Relation از آن استفاده کنیم.

---

## قابلیت Query قدرتمند

با SQL می‌توانیم:

* جستجو کنیم
* مرتب کنیم
* فیلتر کنیم
* گزارش بسازیم
* داده‌ها را ترکیب کنیم

---

## حفظ Integrity

Database می‌تواند قوانین مشخصی برای داده‌ها تعریف کند.

مثلا:

ایمیل تکراری نباشد.

سن منفی نباشد.

کاربر بدون شناسه ایجاد نشود.

---

# تمرین

## تمرین ۱

برای یک سیستم مدرسه، جدول‌های مناسب طراحی کنید.

حداقل ۳ جدول پیشنهاد دهید.

---

## تمرین ۲

در جدول زیر مشخص کنید هر مورد چیست:

```
Students

id | name | grade
------------------
1  | Ali  | A
```

موارد:

* Students
* id
* Ali
* کل اطلاعات Ali

---

## تمرین ۳

برای یک فروشگاه اینترنتی مشخص کنید چه Tableهایی نیاز داریم.

---

<br>

---

# 96.5 - طراحی جدول

طراحی جدول یکی از مهم‌ترین مهارت‌ها در Database است.

یک طراحی اشتباه می‌تواند باعث:

* داده‌های تکراری
* کاهش سرعت
* مشکلات نگهداری
* خطاهای منطقی

شود.

---

# انتخاب Columnها

اولین مرحله در طراحی Table مشخص کردن ویژگی‌هایی است که نیاز داریم.

مثلا برای کاربران:

اطلاعات:

```
نام
ایمیل
رمز عبور
تاریخ ثبت نام
شماره تلفن
```

پس:

```
Users

id
name
email
password
phone
created_at
```

---

# Data Type

هر Column باید نوع داده مشخصی داشته باشد.

مثلا:

```
age INTEGER

email VARCHAR

created_at TIMESTAMP
```

انتخاب درست Type باعث:

* مصرف کمتر حافظه
* سرعت بیشتر
* جلوگیری از داده اشتباه

می‌شود.

---

# Primary Key

Primary Key یک ستون برای شناسایی یکتا هر Record است.

مثلا:

```
Users

id | name
---------
1  | Ali
2  | Sara
```

ستون:

```
id
```

Primary Key است.

ویژگی‌ها:

* تکراری نیست
* مقدار Null ندارد
* هر Record یک مقدار متفاوت دارد

---

# Unique Value

Unique یعنی مقدار یک ستون نمی‌تواند تکراری باشد.

مثلا:

ایمیل کاربران:

```
ali@test.com
sara@test.com
```

اما:

```
ali@test.com
ali@test.com
```

مجاز نیست.

---

# Default Value

Default یعنی مقدار پیش‌فرض.

مثلا:

```
is_active BOOLEAN DEFAULT true
```

اگر هنگام ساخت کاربر مقدار ارسال نشود:

```
is_active = true
```

قرار می‌گیرد.

---

# Nullable

Nullable مشخص می‌کند یک ستون می‌تواند مقدار خالی داشته باشد یا نه.

مثلا:

شماره تلفن اختیاری:

```
phone NULL
```

اما ایمیل ضروری:

```
email NOT NULL
```

---

# مثال طراحی جدول Users

```
Users

id
----------------
Primary Key

name
----------------
VARCHAR
NOT NULL

email
----------------
VARCHAR
UNIQUE

password
----------------
VARCHAR
NOT NULL

created_at
----------------
TIMESTAMP
DEFAULT NOW()
```

---

# اشتباهات رایج

## ذخیره کردن چند مقدار در یک Column

اشتباه:

```
phone

0912,0935,0991
```

بهتر:

ساخت جدول جدا:

```
UserPhones
```

---

## انتخاب Type اشتباه

مثلا:

ذخیره سن:

```
VARCHAR
```

در حالی که:

```
INTEGER
```

مناسب است.

---

## نداشتن Primary Key

هر جدول باید روش مشخصی برای شناسایی Recordها داشته باشد.

---

# نکات حرفه‌ای

* همیشه برای جدول‌ها Primary Key قرار دهید.
* نام Columnها واضح انتخاب شود.
* اطلاعات تکراری را جدا کنید.
* از Nullable بی‌دلیل استفاده نکنید.
* قبل از ساخت Database طراحی کنید.

---

# تمرین

## تمرین ۱

جدول کاربران یک سایت را طراحی کنید.

ستون‌ها:

* نام
* ایمیل
* رمز عبور
* تاریخ ثبت نام
* وضعیت فعال بودن

---

## تمرین ۲

برای محصول فروشگاه جدول طراحی کنید.

اطلاعات:

* نام محصول
* قیمت
* موجودی
* تاریخ ایجاد

---

## تمرین ۳

مشخص کنید کدام ستون باید:

* Primary Key
* Unique
* Nullable
* Default

باشد.

---

<br>

---

# 96.6 - Data Types در SQL

هر Database برای ذخیره داده‌ها از Data Type استفاده می‌کند.

انتخاب درست Type اهمیت زیادی دارد.

---

# Integer

برای اعداد صحیح استفاده می‌شود.

مثال:

```
age INTEGER
```

مقادیر:

```
10
25
100
```

---

# Decimal

برای اعداد دقیق استفاده می‌شود.

مناسب برای:

* پول
* قیمت
* حسابداری

مثال:

```
price DECIMAL(10,2)
```

مقدار:

```
99.99
```

---

# Float

برای اعداد اعشاری استفاده می‌شود.

مثال:

```
temperature FLOAT
```

اما برای پول معمولا پیشنهاد نمی‌شود.

---

# Boolean

برای مقدارهای درست و غلط.

مقادیر:

```
true
false
```

مثال:

```
is_active BOOLEAN
```

---

# Char

برای متن با طول ثابت.

مثال:

```
country_code CHAR(2)
```

مقدار:

```
IR
```

---

# Varchar

برای متن با طول متغیر.

مثال:

```
username VARCHAR(50)
```

مناسب برای:

* نام
* ایمیل
* شماره تلفن

---

# Text

برای متن‌های طولانی.

مثال:

```
description TEXT
```

استفاده برای:

* مقاله
* توضیحات محصول
* کامنت

---

# Date

فقط تاریخ.

مثال:

```
2026-08-07
```

---

# Time

فقط زمان.

مثال:

```
14:30:00
```

---

# Timestamp

تاریخ و زمان کامل.

مثال:

```
2026-08-07 14:30:00
```

کاربرد:

* created_at
* updated_at
* logs

---

# JSON

برای ذخیره داده‌های JSON.

مثال:

```json
{
 "color":"red",
 "size":"XL"
}
```

کاربرد:

* تنظیمات
* Metadata
* داده‌های انعطاف‌پذیر

---

# مقایسه Data Typeها

| Type      | کاربرد           |
| --------- | ---------------- |
| Integer   | عدد صحیح         |
| Decimal   | عدد دقیق         |
| Float     | عدد اعشاری       |
| Boolean   | درست/غلط         |
| Char      | متن ثابت         |
| Varchar   | متن کوتاه        |
| Text      | متن طولانی       |
| Date      | تاریخ            |
| Time      | زمان             |
| Timestamp | تاریخ و زمان     |
| JSON      | داده ساختاریافته |

---

# تمرین

## تمرین ۱

برای هر مورد Type مناسب انتخاب کنید:

1. قیمت محصول
2. نام کاربر
3. وضعیت فعال بودن
4. تاریخ ثبت سفارش
5. توضیحات مقاله

---

## تمرین ۲

جدول زیر را کامل کنید:

```
Products

id
name
price
description
created_at
is_available
```

برای هر Column یک Data Type انتخاب کنید.

---

## تمرین ۳

تفاوت این موارد را توضیح دهید:

* Char و Varchar
* Decimal و Float
* Date و Timestamp

---

# پاسخ تشریحی تمرین‌ها

## پاسخ تمرین 96.4

نمونه جدول فروشگاه:

```
Users
Products
Orders
```

---

## پاسخ تمرین 96.5

نمونه:

```
Users

id INTEGER PRIMARY KEY

name VARCHAR NOT NULL

email VARCHAR UNIQUE

password VARCHAR NOT NULL

created_at TIMESTAMP DEFAULT NOW()
```

---

## پاسخ تمرین 96.6

نمونه:

```
price -> Decimal

name -> Varchar

is_active -> Boolean

created_at -> Timestamp

description -> Text
```

---

# بخش ۳ - نصب و آماده‌سازی محیط SQL

## 96.7 - نصب Database Server

برای کار با SQL و اتصال آن به پروژه‌های Node.js ابتدا باید یک Database Server را روی سیستم خود نصب کنیم. در این بخش با محبوب‌ترین Databaseهای رابطه‌ای آشنا می‌شویم و نحوه آماده‌سازی محیط توسعه را یاد می‌گیریم.

---

## PostgreSQL

PostgreSQL Global Development Group

### معرفی PostgreSQL

* PostgreSQL چیست؟
* چرا PostgreSQL یکی از محبوب‌ترین Databaseهای دنیا است؟
* تفاوت PostgreSQL با سایر Databaseهای رابطه‌ای
* ویژگی‌های PostgreSQL:

  * Open Source بودن
  * پشتیبانی از استانداردهای SQL
  * امنیت بالا
  * قابلیت توسعه
  * پشتیبانی از JSON
  * پشتیبانی از Transaction
  * مناسب برای پروژه‌های بزرگ

---

### نصب PostgreSQL

* نصب در Windows
* نصب در Linux
* نصب در macOS
* بررسی نصب صحیح
* آشنایی با PostgreSQL Service

---

### ابزارهای PostgreSQL

* PostgreSQL Server
* PostgreSQL Client
* psql Command Line
* pgAdmin

---

### اولین اتصال به PostgreSQL

* ساخت User
* ساخت Database
* اتصال به Database
* اجرای اولین Query

مثال:

```sql
CREATE DATABASE test_db;
```

اتصال:

```bash
psql -U postgres -d test_db
```

---

### دستورات پایه PostgreSQL

* مشاهده Databaseها
* انتخاب Database
* مشاهده Tableها
* خروج از محیط psql
* اجرای فایل SQL

---

تمرین

---

# MySQL

Oracle MySQL

## معرفی MySQL

* MySQL چیست؟
* تاریخچه MySQL
* کاربردهای MySQL
* تفاوت MySQL و PostgreSQL
* چه پروژه‌هایی از MySQL استفاده می‌کنند؟

---

## نصب MySQL

* نصب در Windows
* نصب در Linux
* نصب در macOS
* MySQL Server
* MySQL Client

---

## ابزارهای MySQL

* MySQL Command Line
* MySQL Workbench
* اتصال به Server

---

## اولین Database در MySQL

ساخت Database:

```sql
CREATE DATABASE shop;
```

اتصال:

```sql
USE shop;
```

---

## دستورات پایه MySQL

* نمایش Databaseها
* انتخاب Database
* مشاهده Tableها
* اجرای Query
* خروج از محیط MySQL

---

تمرین

---

# SQLite

SQLite

## معرفی SQLite

* SQLite چیست؟
* تفاوت SQLite با Database Serverها
* Database بدون Server
* ذخیره Database در یک فایل
* چه زمانی از SQLite استفاده کنیم؟

---

## کاربردهای SQLite

* پروژه‌های کوچک
* برنامه‌های Desktop
* Mobile Application
* تست و توسعه اولیه
* پروژه‌های آموزشی

---

## نصب SQLite

* نصب SQLite CLI
* بررسی نصب
* ساخت Database File

---

## اولین Database در SQLite

ساخت Database:

```bash
sqlite3 database.db
```

ایجاد Table:

```sql
CREATE TABLE users (
    id INTEGER PRIMARY KEY,
    name TEXT
);
```

---

## تفاوت PostgreSQL، MySQL و SQLite

| ویژگی              | PostgreSQL      | MySQL           | SQLite            |
| ------------------ | --------------- | --------------- | ----------------- |
| نوع                | Server Database | Server Database | Embedded Database |
| مناسب پروژه بزرگ   | ✅               | ✅               | ❌                 |
| نیاز به Server     | ✅               | ✅               | ❌                 |
| مناسب آموزش        | ✅               | ✅               | ✅                 |
| پشتیبانی JSON      | عالی            | خوب             | محدود             |
| استفاده در Node.js | بسیار زیاد      | بسیار زیاد      | زیاد              |

---

تمرین

---

# 96.8 - ابزارهای مدیریت Database

برای کار حرفه‌ای با Database فقط Command Line کافی نیست. برنامه‌نویسان معمولاً از ابزارهای گرافیکی برای مدیریت، مشاهده داده‌ها و نوشتن Query استفاده می‌کنند.

---

# Command Line Client

## مفهوم Command Line Database Tools

* CLI چیست؟
* مزایا و معایب کار با Terminal
* اتصال مستقیم به Database
* اجرای Query
* مدیریت Database بدون رابط گرافیکی

---

## ابزارهای مهم CLI

### PostgreSQL

```bash
psql
```

### MySQL

```bash
mysql
```

### SQLite

```bash
sqlite3
```

---

تمرین

---

# Database GUI

## مفهوم ابزارهای گرافیکی Database

* چرا از GUI استفاده می‌کنیم؟
* مشاهده ساختار Database
* مدیریت Tableها
* اجرای Query
* Export و Import اطلاعات
* مدیریت کاربران

---

# Query Editor

## Query Editor چیست؟

* محیط نوشتن SQL
* Syntax Highlighting
* اجرای Query
* نمایش نتیجه
* ذخیره Queryهای پرکاربرد

---

# معرفی ابزارهای مدیریت Database

---

## pgAdmin

pgAdmin

### معرفی

* ابزار رسمی مدیریت PostgreSQL
* رابط گرافیکی برای PostgreSQL
* مدیریت Server
* ساخت Database
* مدیریت Table
* اجرای SQL Query

---

### امکانات pgAdmin

* Browser Database
* Query Tool
* Backup و Restore
* مدیریت Userها
* مشاهده Performance

---

تمرین

---

# DBeaver

DBeaver

## معرفی

* ابزار چندمنظوره Database
* پشتیبانی از Databaseهای مختلف
* مناسب برای توسعه‌دهندگان Backend

---

## Databaseهای قابل پشتیبانی

* PostgreSQL
* MySQL
* SQLite
* SQL Server
* Oracle
* MongoDB

---

## امکانات DBeaver

* مدیریت چند Database همزمان
* ER Diagram
* Query Editor
* Export داده‌ها
* Import داده‌ها
* نمایش ساختار Tableها

---

تمرین

---

# TablePlus

TablePlus

## معرفی

* ابزار مدرن مدیریت Database
* سرعت بالا
* رابط کاربری ساده
* مناسب توسعه‌دهندگان Backend

---

## امکانات TablePlus

* اتصال سریع به Database
* Query Editor
* مدیریت Tableها
* مشاهده و ویرایش داده‌ها
* پشتیبانی از چند Database

---

تمرین

---

# انتخاب ابزار مناسب برای Node.js Developer

## برای شروع یادگیری

پیشنهاد:

* PostgreSQL + pgAdmin

---

## برای کار حرفه‌ای

پیشنهاد:

* PostgreSQL
* DBeaver
* Terminal

---

## برای پروژه‌های کوچک

پیشنهاد:

* SQLite

---

# تمرین‌های بخش ۳

## تمرین ۱

PostgreSQL را نصب کنید و:

* یک Database بسازید
* به آن متصل شوید
* یک Table ایجاد کنید

---

## تمرین ۲

با استفاده از Command Line:

* Databaseها را نمایش دهید
* یک Database حذف کنید
* یک Database جدید ایجاد کنید

---

## تمرین ۳

DBeaver را نصب کنید و:

* PostgreSQL را به آن متصل کنید
* یک Query اجرا کنید
* نتیجه را مشاهده کنید

---

## تمرین ۴

سه Database زیر را مقایسه کنید:

* PostgreSQL
* MySQL
* SQLite

و بنویسید برای چه پروژه‌ای هرکدام را انتخاب می‌کنید.

---

## تمرین ۵

یک محیط توسعه آماده کنید:

* Node.js
* PostgreSQL
* DBeaver
* VS Code

و بررسی کنید همه ابزارها آماده کار هستند.

---

# بخش ۴ - دستورات پایه SQL

## 96.9 - ساخت Database

Database اولین بخش از کار با سیستم‌های مدیریت پایگاه داده است. قبل از اینکه بتوانیم Table ایجاد کنیم و اطلاعات ذخیره کنیم، باید یک Database بسازیم.

در این بخش یاد می‌گیریم چگونه:

* یک Database جدید ایجاد کنیم
* Databaseهای موجود را مشاهده کنیم
* Database را انتخاب کنیم
* Database را حذف کنیم
* تفاوت حذف Database در محیط‌های مختلف را درک کنیم

---

# CREATE DATABASE

دستور `CREATE DATABASE` برای ساخت یک پایگاه داده جدید استفاده می‌شود.

ساختار کلی:

```sql
CREATE DATABASE database_name;
```

مثال:

```sql
CREATE DATABASE shop;
```

با اجرای این دستور یک Database با نام `shop` ساخته می‌شود.

---

## نام‌گذاری Database

هنگام انتخاب نام Database باید به قوانین زیر توجه کنیم:

* نام باید معنی‌دار باشد
* از فاصله استفاده نشود
* از کاراکترهای خاص کمتر استفاده شود
* بهتر است از حروف کوچک استفاده شود

نمونه مناسب:

```text
user_management
online_shop
blog_system
company_database
```

نمونه نام‌های نامناسب:

```text
my database
123database
test!!!
```

---

# مشاهده Database ها

برای مشاهده Databaseهای موجود:

## PostgreSQL

```sql
\l
```

یا:

```sql
SELECT datname FROM pg_database;
```

---

## MySQL

```sql
SHOW DATABASES;
```

خروجی نمونه:

```
+----------------+
| Database       |
+----------------+
| shop           |
| users          |
| products       |
+----------------+
```

---

# انتخاب Database

بعد از ساخت Database باید آن را انتخاب کنیم تا دستورات بعدی روی همان Database اجرا شوند.

## MySQL

```sql
USE shop;
```

بعد از این دستور تمام عملیات روی Database `shop` انجام می‌شود.

---

## PostgreSQL

در PostgreSQL انتخاب Database معمولاً هنگام اتصال انجام می‌شود:

```bash
psql -d shop
```

یا:

```bash
\c shop
```

---

# حذف Database

برای حذف کامل یک Database:

```sql
DROP DATABASE database_name;
```

مثال:

```sql
DROP DATABASE shop;
```

بعد از اجرا:

* تمام Tableها حذف می‌شوند
* تمام داده‌ها حذف می‌شوند
* امکان بازگردانی معمولاً وجود ندارد

---

# تفاوت DELETE و DROP در Database

| دستور         | کاربرد                       |
| ------------- | ---------------------------- |
| DROP DATABASE | حذف کامل Database            |
| DELETE        | حذف داده‌های داخل Table      |
| TRUNCATE      | حذف سریع تمام داده‌های Table |

---

# نکات مهم حرفه‌ای

* قبل از اجرای `DROP DATABASE` حتماً Backup بگیرید.
* در محیط Production هیچ دستوری را بدون بررسی اجرا نکنید.
* Databaseهای اصلی پروژه معمولاً حذف نمی‌شوند.
* بهتر است برای تست Database جداگانه ساخته شود.

---

# تمرین 96.9

## تمرین ۱

یک Database با نام زیر بسازید:

```
library
```

---

## تمرین ۲

یک Database با نام:

```
online_store
```

بسازید و سپس آن را انتخاب کنید.

---

## تمرین ۳

Database زیر را حذف کنید:

```
test_database
```

---

# پاسخ تمرین 96.9

## پاسخ تمرین ۱

```sql
CREATE DATABASE library;
```

---

## پاسخ تمرین ۲

ساخت Database:

```sql
CREATE DATABASE online_store;
```

انتخاب:

MySQL:

```sql
USE online_store;
```

PostgreSQL:

```sql
\c online_store
```

---

## پاسخ تمرین ۳

```sql
DROP DATABASE test_database;
```

---

---

# 96.10 - ساخت Table

Table اصلی‌ترین ساختار ذخیره اطلاعات در Databaseهای رابطه‌ای است.

در این بخش یاد می‌گیریم:

* Table چیست
* چگونه Table بسازیم
* Column تعریف کنیم
* Data Type انتخاب کنیم
* Constraint اضافه کنیم

---

# CREATE TABLE

برای ساخت Table از دستور:

```sql
CREATE TABLE
```

استفاده می‌کنیم.

ساختار کلی:

```sql
CREATE TABLE table_name (
    column_name data_type
);
```

---

مثال:

```sql
CREATE TABLE users (
    id INT,
    name VARCHAR(100)
);
```

Table ساخته شده:

```
users

id      name
----------------
1       Ali
2       Sara
```

---

# تعریف Column

هر Table از چند Column تشکیل شده است.

مثال:

```sql
CREATE TABLE products (
    id INT,
    title VARCHAR(200),
    price INT
);
```

Columnها:

| Column | Type    |
| ------ | ------- |
| id     | INT     |
| title  | VARCHAR |
| price  | INT     |

---

# Data Type در Column

هنگام ساخت Column باید نوع داده مشخص شود.

مثال:

```sql
age INT
```

یعنی فقط عدد ذخیره می‌کند.

نمونه‌های رایج:

```sql
INT
VARCHAR
TEXT
BOOLEAN
DATE
TIMESTAMP
DECIMAL
```

---

# Constraint ها

Constraint برای کنترل داده‌ها استفاده می‌شود.

مهم‌ترین Constraintها:

* PRIMARY KEY
* UNIQUE
* NOT NULL
* DEFAULT
* FOREIGN KEY

---

## PRIMARY KEY

هر Table معمولاً یک شناسه اصلی دارد.

مثال:

```sql
CREATE TABLE users (
    id INT PRIMARY KEY,
    name VARCHAR(100)
);
```

ویژگی‌ها:

* تکراری نیست
* NULL نیست
* هر رکورد را مشخص می‌کند

---

## NOT NULL

اجازه خالی بودن نمی‌دهد.

مثال:

```sql
name VARCHAR(100) NOT NULL
```

---

## UNIQUE

مقدار تکراری قبول نمی‌کند.

مثال:

```sql
email VARCHAR(200) UNIQUE
```

---

## DEFAULT

مقدار پیش‌فرض تعیین می‌کند.

مثال:

```sql
status VARCHAR(20) DEFAULT 'active'
```

---

# مثال کامل ساخت Table

```sql
CREATE TABLE users (
    id INT PRIMARY KEY,
    username VARCHAR(50) NOT NULL,
    email VARCHAR(100) UNIQUE,
    age INT,
    status VARCHAR(20) DEFAULT 'active'
);
```

---

# تمرین 96.10

## تمرین ۱

یک Table با نام:

```
students
```

بسازید که شامل موارد زیر باشد:

* id
* name
* age

---

## تمرین ۲

یک Table برای محصولات بسازید:

```
products
```

شامل:

* id
* title
* price
* stock

---

## تمرین ۳

برای کاربران یک Table بسازید که:

* id کلید اصلی باشد
* email تکراری نباشد
* username خالی نباشد

---

# پاسخ تمرین 96.10

## پاسخ تمرین ۱

```sql
CREATE TABLE students (
    id INT PRIMARY KEY,
    name VARCHAR(100),
    age INT
);
```

---

## پاسخ تمرین ۲

```sql
CREATE TABLE products (
    id INT PRIMARY KEY,
    title VARCHAR(200),
    price INT,
    stock INT
);
```

---

## پاسخ تمرین ۳

```sql
CREATE TABLE users (
    id INT PRIMARY KEY,
    username VARCHAR(50) NOT NULL,
    email VARCHAR(100) UNIQUE
);
```

---

# 96.11 - تغییر ساختار Table

بعد از ساخت Table ممکن است نیاز داشته باشیم ساختار آن را تغییر دهیم.

برای این کار از:

```sql
ALTER TABLE
```

استفاده می‌کنیم.

---

# ADD COLUMN

اضافه کردن Column جدید:

```sql
ALTER TABLE users
ADD COLUMN phone VARCHAR(20);
```

بعد از اجرا:

```
users

id
username
email
phone
```

---

# DROP COLUMN

حذف یک Column:

```sql
ALTER TABLE users
DROP COLUMN phone;
```

هشدار:

با حذف Column تمام اطلاعات آن حذف می‌شود.

---

# MODIFY COLUMN

تغییر نوع Column.

## MySQL

```sql
ALTER TABLE users
MODIFY COLUMN username VARCHAR(200);
```

---

# Rename Column

تغییر نام Column:

## MySQL

```sql
ALTER TABLE users
RENAME COLUMN username TO name;
```

---

## PostgreSQL

```sql
ALTER TABLE users
RENAME COLUMN username TO name;
```

---

# تمرین 96.11

## تمرین ۱

به Table کاربران یک Column اضافه کنید:

```
birth_date
```

---

## تمرین ۲

Column زیر را حذف کنید:

```
phone
```

---

## تمرین ۳

نام Column زیر را تغییر دهید:

```
username
```

به:

```
full_name
```

---

# پاسخ تمرین 96.11

## پاسخ تمرین ۱

```sql
ALTER TABLE users
ADD COLUMN birth_date DATE;
```

---

## پاسخ تمرین ۲

```sql
ALTER TABLE users
DROP COLUMN phone;
```

---

## پاسخ تمرین ۳

```sql
ALTER TABLE users
RENAME COLUMN username TO full_name;
```

---

# 96.12 - حذف داده و جدول

در SQL چند دستور برای حذف وجود دارد که عملکرد متفاوتی دارند:

* DROP
* DELETE
* TRUNCATE

شناخت تفاوت این دستورات بسیار مهم است.

---

# DROP

برای حذف کامل ساختار استفاده می‌شود.

مثال:

```sql
DROP TABLE users;
```

نتیجه:

* Table حذف می‌شود
* داده‌ها حذف می‌شوند
* ساختار Table از بین می‌رود

---

# DELETE

برای حذف رکوردها استفاده می‌شود.

مثال:

```sql
DELETE FROM users;
```

تمام داده‌ها حذف می‌شوند اما Table باقی می‌ماند.

---

حذف شرطی:

```sql
DELETE FROM users
WHERE id = 5;
```

فقط کاربر شماره ۵ حذف می‌شود.

---

# TRUNCATE

برای حذف سریع تمام داده‌های Table:

```sql
TRUNCATE TABLE users;
```

ویژگی‌ها:

* همه رکوردها حذف می‌شوند
* ساختار Table باقی می‌ماند
* سریع‌تر از DELETE است

---

# تفاوت DROP، DELETE و TRUNCATE

| ویژگی      | DROP | DELETE | TRUNCATE |
| ---------- | ---- | ------ | -------- |
| حذف Table  | ✅    | ❌      | ❌        |
| حذف داده   | ✅    | ✅      | ✅        |
| حذف ساختار | ✅    | ❌      | ❌        |
| امکان شرط  | ❌    | ✅      | ❌        |
| سرعت       | زیاد | کمتر   | زیاد     |

---

# اشتباهات رایج

* استفاده از DROP روی Database اصلی
* اجرای DELETE بدون WHERE
* اشتباه گرفتن TRUNCATE و DROP
* حذف اطلاعات بدون Backup

---

# نکات حرفه‌ای

* در پروژه‌های واقعی قبل از حذف داده Backup بگیرید.
* برای حذف چند رکورد مشخص از DELETE استفاده کنید.
* برای خالی کردن کامل Table از TRUNCATE استفاده کنید.
* برای حذف کامل موجودیت از DROP استفاده کنید.

---

# تمرین 96.12

## تمرین ۱

تمام کاربران را حذف کنید ولی Table باقی بماند.

---

## تمرین ۲

کاربری با id برابر 10 را حذف کنید.

---

## تمرین ۳

Table زیر را کاملاً حذف کنید:

```
logs
```

---

# پاسخ تمرین 96.12

## پاسخ تمرین ۱

```sql
DELETE FROM users;
```

یا:

```sql
TRUNCATE TABLE users;
```

---

## پاسخ تمرین ۲

```sql
DELETE FROM users
WHERE id = 10;
```

---

## پاسخ تمرین ۳

```sql
DROP TABLE logs;
```


---

